# PROJECT-NOTES — 삼십초 역사 (toon-pilot)

2026-07-04 세션 핸드오프. 사우스파크풍 종이 컷아웃 애니메이션을 코드로 빠르게 찍어내는 파일럿.
목표 채널: 유튜브 역사/영화/시사 30초~ 요약 (오리지널 캐릭터, South Park 디자인 복제 금지).

## 완성물

- `renders/imjin-30s-pilot.mp4` — 35.5s, 1920×1080, h264+aac. "임진왜란 편" 파일럿 완성본.
- `renders/imjin-30s-pilot-v2.mp4` — 동일 에피소드, 파츠 일러스트 업그레이드 버전 (2026-07-05).
- 스타일 스펙은 `design.md` (팔레트·폰트·모션 규칙·금지사항).

## 아키텍처 (3-레이어 분리가 핵심)

| 파일 | 역할 |
| --- | --- |
| `characters.js` | **rig 팩토리.** `makeCharacter({variant, skin, shirt, pants})` 하나로 모든 캐릭터 생성. variant = host/king/admiral/militia (모자·머리만 교체). `EXPRESSIONS` 5종(neutral/happy/proud/shocked/angry) = 눈·입·눈썹 가시성/변형 조합. `POSES` 4종(down/point/raised/shrug) = 팔 회전각. |
| `scenes.js` | **디렉터 장면 JSON.** 장면별 start/dur/talker/chars(위치·표정·포즈)/changes(중간 연출 변경). LLM이 생성하는 레이어가 여기 + 대본. `TRANSITION=0.45` 겹침 포함. |
| `captions-data.js` | whisper 타이밍 + 원문 보정된 `WORDS`(립싱크 구동) & `CAPTION_GROUPS`(자막, hi=강조어). |
| `index.html` | 조립. 장면 DOM/배경, 캐릭터 마운트, 타임라인 빌드(입 플랩·깜빡임·바운스·전환·자막·스탬프). |

## 에피소드 제작 파이프라인

```bash
# 1. 대본 (자연 발화 ~2.5w/s, 숫자는 한글 표기 — TTS가 읽는 대로)
vim script.txt
# 2. 한국어 TTS — 내장 Kokoro는 한국어 미지원, edge-tts 사용
uvx edge-tts --voice ko-KR-InJoonNeural --rate=+6% --file script.txt --write-media narration.mp3
# 3. 단어 타임스탬프 (.en 모델 금지)
npx hyperframes transcribe narration.mp3 --model small --language ko
# 4. transcript.json 타이밍으로 captions-data.js(WORDS/GROUPS) + scenes.js(장면 경계=문장 경계) 갱신
#    index.html의 각 씬 data-start/duration, audio data-duration, root data-duration도 동기화
# 5. 검증 → 렌더
npm run check
npx hyperframes render --quality standard --output renders/<name>.mp4
# 프레임 눈검증: ffmpeg -ss <t> -i renders/x.mp4 -frames:v 1 f.png
```

첫 편 기준 렌더 ~34초. 대본→완성 몇 분 수준.

## 세션에서 밟은 지뢰 (재발 방지)

1. **SVG 팔 회전 피벗**: `<g>`에 CSS `transform-origin: Npx Npx` 쓰면 피벗이 틀어져 팔이 몸에서 분리됨(스케일된 svg에서 단위 해석 문제). 반드시 GSAP `svgOrigin` 사용 — arm-l `"60 152"`, arm-r `"140 152"` (초기 포즈 `gsap.set` + 변경 `tl.to` 모두).
2. **inspector는 invisible도 측정**: `tl.fromTo`의 immediateRender가 등장 전 pre-state(거대 scale, opacity 0)를 만들고, inspector는 `visibility:hidden`이어도 잡음. 스탬프류 슬램은 **scale 0→1 back.out** + CSS `visibility:hidden` + 등장 시점 `tl.set visible`로.
3. **자식 오버플로가 부모 씬에 귀속됨**: 캔버스보다 큰 장식(드리프트용 2400px 파도)은 씬의 scrollWidth를 부풀려 `clipped_text` 에러 유발. `data-layout-ignore`를 자식에 달아도 소용없음 — **overflow:hidden 래퍼**(`.waves-clip`, data-layout-ignore) 안에 넣어야 함. 오프스크린 진입(거북선)도 동일 래퍼로 해결.
4. **잔여 inspect 경고 5건은 노이즈**: #s3 풀캔버스 rect에 스탬프 텍스트가 귀속돼 brand-badge/자막과 "겹침"으로 나옴. 렌더 프레임으로 실겹침 없음 확인(2026-07-04). 경고 카운트가 이 기준선에서 늘면 그때만 조사.
5. **transcript.json 한글이 깨져 보이는 것**은 터미널 출력 인코딩 문제일 수 있음 — 원문은 script.txt 기준으로 captions-data.js에 보정해 둠. whisper가 단어를 합치기도 함(예: "이십 일"→"20일") — 단어 수 대조로 매핑.
6. **장면 전환은 트랙 교대**: same-track 클립은 겹칠 수 없으므로 씬을 track 0/1 교대 배치 + 0.45s 겹침 + z-index 상승(#s1 10 → #s5 50, overlay 100). 진입 씬의 `.stage`(배경 포함 풀블리드)를 push/iris/blur로 덮어씌움. 퇴장 애니메이션 금지(마지막 씬 제외).
7. **preview 서버**: 프로젝트 루트가 아닌 세션에서는 `.claude/launch.json`의 `toon-pilot-preview`(port 3013) 사용. preview_eval로 `window.__timelines.main.seek(t)` 후 DOM 검사 가능 (overlay가 최상단이므로 `elementsFromPoint` 복수형).
8. **지뢰 #2는 EP1 자체 코드에서도 안 지켜진 곳이 있었음**(hook-stamp/stamp-rank/stamp-flog가 `scale:2.6~2.8→1`로 슬램). EP2(김시습)에서 이 패턴을 그대로 복붙했다가 `clipped_text` 에러 재현(2026-07-09) — 큰 텍스트 스탬프는 예외 없이 **scale 0→1**만 쓸 것. 기존 씬 복사 시 이 지뢰가 반복될 수 있으니 스탬프 애니메이션은 매번 확인.
9. **TTS 특정 단어 발음 실패**: edge-tts `ko-KR-InJoonNeural`이 "금오"(금오산·금오신화·금오공과대학교 등)를 문맥·보이스 무관하게 "금호"/무작위 오발음으로 재현(2026-07-09, SunHi/Hyunsu 보이스로도 재현 — 특정 보이스 문제 아님). 표기 변경(띄어쓰기·구두점)으로도 해결 안 됨 — **동의어로 우회**(예: 금오산→"경주 남산")하거나, 발음이 필수가 아니면 대사에서 빼고 화면 스탬프로만 노출. 새 대본에 흔치 않은 한자어 고유명사가 들어가면 TTS 생성 후 whisper 재전사로 반드시 귀검증(S4).
10. **자막 숫자 변환은 작은 수도 빠뜨리지 말 것**: S5 "표시형 변환"(십오만→15만)을 큰 수에만 적용하고 나이·기간 같은 작은 수(다섯 살→5살, 사흘→3일)는 임의로 생략했다가 사용자 피드백으로 재작업(2026-07-10). 대사(TTS용)는 한글 표기 유지, 자막(CAPTION_GROUPS)만 숫자로 변환.
11. **직업/신분 전환은 모자까지 갈아입혀야 함**: 캐릭터가 승려·삭발 등으로 신분이 바뀌는 서사인데 rig의 모자(갓)를 그대로 두면 "아직도 갓 쓰고 있다"는 피드백 발생(2026-07-10). `characters.js`에 `cfg.noHat`(민머리 대체) 옵션으로 대응. **삭발 승려는 `militia`와 같은 variant지만 별도 튜닝 키 `"monk"`로 관리** — `characters.js`가 `variant==="militia" && cfg.noHat`이면 `tuneKey="monk"`로 해석하고, noHat일 때도 (원래는 건너뛰던) `wrapTf`를 삭발 음영에 적용(pivotY=72). 갓 쓴 militia와 앉음새를 독립 조정하기 위함. `studio.html`에 5번째 탭 "승려(삭발)"로 노출(`CHAR_TUNING.monk` 필요 — 구버전 localStorage 대비 로드시 누락 키 자동 채움).
12. **CHAR_TUNING 값은 씬마다 재검증**: militia 갓의 `y:-20,s:1.3`은 실제로 렌더해보니 크라운이 머리 위로 크게 떠 보임(피벗 계산상 뷰박스 밖으로 나갈 정도). 이전 세션이 정한 튜닝값이라도 새 에피소드 렌더 프레임에서 반드시 육안 재확인할 것 — `y:-4,s:1.05` 정도가 갓이 자연스럽게 앉는 값.
    **2026-07-25(EP.19) 확정값 = `militia.hat {x:0, y:-16, s:1.58, r:0}`** — 사용자가 `studio.html`로 직접 조정해 확정(`admiral.beard`도 `{y:-3, s:1.05}` 동시 갱신). **지배 변수는 `y`가 아니라 `s`(크기)다.** 갓이 어색한 주원인은 위치가 아니라 **양태(챙)가 좁아 머리를 못 덮은 것**이고(s1.58 → 챙 rx 68→107 = 머리 r58의 1.85배), 챙을 키우면 y는 오히려 −16으로 올려야 맞는다. 본 세션이 `y`만 6단계 훑고 `s`를 1.0 근처로 고정한 채 "`y:0, s:1.05`가 정답"이라 결론냈던 것은 **오진**이었다 — 스윕은 지배 변수를 포함해야 하고, 한 축만 훑으면 그 축의 국소 최적을 정답으로 착각한다. **rig 파츠 앉음새의 최종 판정자는 `studio.html`을 쓰는 사용자다**(S6.5 배치 원칙의 rig 판). ⚠ king(익선관 `y:-26, s:0.9`)은 미검증 — 해당 variant를 쓰는 다음 편에서 사용자 튜너로 재확인.

54. **index.html을 새로 쓸 때 `<script src="char-tuning.js">`를 빠뜨리면 rig 튜닝이 통째로 죽는데, lint·validate·inspect는 0 에러로 통과한다**(2026-07-25, EP.19). 사용자가 "S1~S7 모든 인물의 갓 위치가 알맞지 않다"고 지적. `char-tuning.js` 값을 고쳐도 렌더가 안 바뀌길래 파일·localStorage를 대조했더니 둘 다 일치 — 진범은 **index.html의 스크립트 로드 목록에서 `char-tuning.js`가 누락된 것**이었다(직전 EP.18 커밋본에는 있었고, 에피소드마다 index.html을 재작성하면서 소실). `characters.js`는 `typeof CHAR_TUNING !== "undefined"`로 방어하고 있어 **조용히 `TU=null`로 떨어져 튜닝을 전부 무시**한다 — 에러도 콘솔 경고도 없다.

55. **에피소드 길이는 어절 수가 아니라 "어절 × 0.49s + 문장 수 × 1.18s"로 결정된다 — 스킬의 2.5어절/s 가정은 틀렸다**(2026-07-25, EP.21). 청취 가독성 규칙대로 "한 문장 = 정보 1개"로 쓴 초안(109어절/20문장)을 TTS로 뽑으니 **76.9초**가 나왔다(기존 편은 47~49초). 원인은 속도가 아니라 **문장 경계마다 붙는 약 1.2초의 쉼**이다 — 짧은 문장을 많이 쓰면 어절 수가 같아도 길이가 는다. 실측: EP.20(70어절/10문장)=47.1s, 초안(109/20)=76.9s, 중간안(80/17)=59.2s, 최종(69/16)=53.2s → 계수 a=0.49s/어절, b=1.18s/문장으로 3점 모두 ±1s 내 예측. **줄일 때는 문장을 압축하지 말고(가독성 규칙 위반) 정보를 덜어내 화면(num-pop·스탬프)으로 옮길 것.** 참고: `--file`의 줄바꿈은 길이에 **전혀** 영향이 없다(16줄 vs 7줄 동일 오디오, 바이트까지 일치).

56. **캐릭터에 붙인 사료 밖 장식 하나가 에피소드의 핵심 사실을 무너뜨릴 수 있다**(2026-07-25, EP.21). 화랑 rig에 이름(花郞)을 반영해 머리 꽃을 달았더니 블라인드 검수 2회가 독립적으로 **"여성으로 오독될 우려"**를 지적했다 — 이 편의 훅이 "미모의 **남자**를 뽑았다"라 성별이 안 읽히면 반전 자체가 죽는다. 같은 화면의 꽃 없는 화랑은 "남성 — 상"으로 통과했으므로 원인은 꽃 단독. **제거 후 남성 판독 회복**(상투 확대 + 머리띠 굵게로 보강). 교훈: **사료에 없는 장식은 넣지 않는다**. 넣고 싶으면 그 장식이 캐릭터의 성별·신분·직업 판독을 흔드는지 먼저 블라인드로 확인할 것.

57. **"신라라서 신라 소품"이 아니다 — 앵커 소품은 그 시대 안에서 다시 확인해야 한다**(2026-07-25, EP.21). 진흥왕 37년(576) 장면의 장소 앵커로 첨성대(sp20)를 세웠는데 고증 게이트가 **선덕여왕대(7세기 중엽) 축조**라고 적발했다 — 같은 신라지만 70년 앞선 장면이라 시대착오다. 대안으로 세운 **왕릉 봉분은 블라인드에서 "천막"으로 판독**돼 폐기(매끈한 반구 + 하단 밝은 호석 띠 = 텐트 실루엣). 결국 큰 소나무로 교체. **소품 팩트체크는 '나라'가 아니라 '연도' 단위로** 할 것(EP.7 엽전 = 조선 전기 금지와 같은 계열).

58. **whisper 정렬은 긴 무음 구간 앞에서 자막을 1초 이상 앞당긴다 — `silencedetect`로 대조 보정할 것**(2026-07-25, EP.21). `sync_timing.py`가 문장 9의 시작을 26.98s로 잡았는데 실제 발화는 28.27s에 시작했다(그 앞 1.38초가 무음). 앞 문장이 끝난 지점을 그대로 다음 문장 시작으로 이어 붙이면서 생긴 오차로, 자막이 내레이션보다 1.3초 먼저 뜬다. 검출: `ffmpeg -af silencedetect=n=-35dB:d=0.30`의 `silence_end`(발화 시작점) 목록과 CAPTION_GROUPS의 start를 대조해 **0.45초 이상 앞선 그룹만** 보정.
    **부수 피해: 이 세션의 "튜닝값 바꿨더니 갓이 내려왔다"는 전후 스냅샷 비교가 통째로 무효였다.** 두 스냅샷 다 튜닝 미적용 상태였는데 기대한 변화를 봤다고 착각했다(확증 편향). **교훈: 설정값을 고친 뒤에는 "그림이 달라 보인다"가 아니라 그 값이 실제로 로드됐는지(스크립트 태그·전역 존재)를 먼저 확인할 것.** 검증법: 렌더 전에 `npx hyperframes inspect` 대신 브라우저에서 `typeof CHAR_TUNING` / `CHAR_TUNING.militia.hat`을 찍어 보거나, index.html 재작성 시 **스크립트 태그 세트를 직전 커밋본과 diff**한다(`git show HEAD:index.html | grep '<script src='`).
13. **배경 소품은 자막 안전존과 겹치는지 렌더 프레임으로 확인**: 정적 인스펙터(`npm run check`)는 자막-소품 겹침을 항상 잡아주지 않음(자막은 JS로 동적 삽입되고 위치도 씬마다 다름). 화면 하단 1/3(`bottom` 값이 작은 소품)에 배경 건물/장식을 놓았다면, 자막이 떠 있는 타임스탬프의 실제 렌더 프레임을 잘라 확인 — 한 번은 암자 건물이 자막에 거의 완전히 가려진 채로 통과할 뻔함.
14. **위치 기반 CSS 선택자(`:last-child`, `:nth-child`) 금지**: 기존 애니메이션이 `.fire-pile path:last-child`처럼 위치로 대상을 잡고 있으면, 나중에 그 컨테이너에 새 SVG 요소를 추가하는 순간 선택자가 의도치 않은 다른 요소를 가리키게 됨. 처음부터 명시적 클래스명을 쓸 것.
15. **GSAP `repeat:-1`(무한 반복) 금지**: 결정론적 프레임 캡처(`hyperframes inspect`/`render`)와 호환되지 않아 린트 에러(`gsap_infinite_repeat`) 발생. 연기·잉걸불처럼 반복 연출이 필요하면 `Math.floor(씬잔여시간 / 사이클시간) - 1`로 유한 repeat 값을 계산해서 쓸 것.
17. **S6.5 튜너 localStorage 오염**(2026-07-13, EP.5): `layout-overrides.js`를 빈 오버라이드로 리셋해도 studio-layout.html이 **localStorage(`toon-layout-overrides-v2`)에 저장된 이전 에피소드 오버라이드를 iframe 위에 복원**해 조정값 회수가 오염된다(EP.5에서 EP.4 키 32개가 섞여 s6-sea를 500px 내리는 등 배치 붕괴). 튜너 오픈 직후 localStorage의 `toon-layout-overrides-v1/v2`·`toon-layout-outline` 제거 + reload(char-tuning-v1은 보존). 회수 시 `__layout.list()` 유효 키로 필터링해 잔여 stale 키 제거. SKILL S6.5에 0번 단계로 명문화.
18. **lady rig 비녀는 담뱃대로 오독**(2026-07-13, EP.5 S8): 스프린트2 lady의 비녀(갈색 막대+구슬)가 flip된 기녀의 턱선에서 짧은 담뱃대로 읽힘 — 블라인드·고증 검수자 2명이 독립적으로 "담뱃대"로 오독. 담배는 임진왜란 이후 유입이라 조선 전기(연산군 등)엔 시대착오. `characters.js` lady 비녀 돌출부 제거로 해결(쪽+한복 V깃+옷고름+치마로 여성 판독은 충분). **위로 솟거나 얼굴 옆으로 뻗는 머리 장식은 원형 캐릭터에서 계속 다른 것으로 오독된다**(뿔=귀, 비녀=담뱃대) — 실루엣 매스로만 표현.
19. **핸드 소품은 rig 기하로 좌표를 계산할 것 — 눈대중은 반드시 뜬다**(2026-07-15, EP.6). 초안이 몽둥이·붓을 손에서 200px 넘게 떨어뜨려 공중 부양(S6.4에서 적발). 계산식: rig viewBox 200×240, 팔 피벗 arm-l `(60,152)`/arm-r `(140,152)`, 손 중심 `(60,200)`/`(140,200)`. `손 = 피벗 + rot(포즈각)·(손중심−피벗)` → `x = charLeft + hx·(w/200)`, `y = (1920−bottom−w·1.2) + hy·(w/200)`. 소품 svg의 자루 끝이 그 좌표에 오도록 left/bottom 역산. **캐릭터가 항상 소품 위에 렌더**(JS가 나중에 append)되므로 몸 바깥으로 뻗은 부분만 보이고, 뻗는 방향의 **다른 캐릭터 갓 챙**(머리 원보다 넓다 — viewBox x 28~172)과 충돌하는지 확인해야 한다. EP.6 s5는 350px 캐릭터 3명이 붙어 있어 갓 챙 사이 통로가 72px뿐 → 위로 뻗는 붓이 안 들어감.
20. **붓은 종이와 붙어 있어야만 붓으로 읽힌다**(2026-07-15, EP.6 S8 — 블라인드 검수자 2명 독립 확인). 맥락 없이 위로 뻗은 붓은 **예외 없이 "창/화살촉/깃대"로 오독**됨(3회 연속 재현). 원인 2가지: ① 기하학적 검은 삼각형 붓끝 = 화살촉 ② 금색 물미 = 창 물미. → 붓끝은 **부드러운 물방울형 검은 붓털**(곡선, 자루 폭 3배), 금색 부속 제거, **종이 위에 눕히거나 붓끝이 종이에 닿게** 배치. s3(더미 위에 눕힘)·s5(종이로 붓끝 내림)는 "붓 — 상" 통과, s6(하늘로 뻗음)은 3번째 시도에서도 "창"이라 **소품을 제거**해 해결. 시그니처 표에 등재.
21. **우산/삿갓 갈림선은 '살 끝의 높이'와 '자루'**(2026-07-15, EP.6 S8). 초안 우산이 "모자 무리 — 정체 불분명"으로 오독. 원인: 살 끝을 가장자리의 **높은** 점에 두어 천이 아래로 처짐 = 삿갓 챙. 실제 우산은 반대 — **살 끝이 낮고 천이 그 사이에서 위로 부푼다**. 여기에 ① 살 끝을 캐노피 밖으로 돌출 ② **중심 자루를 길게 아래로**(모자엔 자루가 없다 = 최대 갈림선)를 더해 재작화 → "우산/파라솔 — 상" 통과.
22. **바닥 소품의 3D 말림은 상자가 된다**(2026-07-15, EP.6 S8). 돗자리에 두꺼운 양끝 말림 + 반쪽 음영을 넣었더니 "나무 상자/궤"로 오독. → **납작한 평행사변형**(원근) + 촘촘한 씨줄/날줄 + 얇은 테두리로 재작화해 해결.
23. **홈서버가 SSH 안 되면 먼저 "Windows 쓰고 계신가요?"를 물을 것 — 듀얼부팅이다**(2026-07-15, EP.6 렌더). 서버에 SSH가 안 붙고 Tailscale 피어가 offline이라 "부팅은 했는데 tailscaled만 죽었다"로 오진했으나, **실제 원인은 사용자가 Windows로 부팅해 쓰고 있던 것**(Tailscale·SSH는 Ubuntu 쪽 서비스라 당연히 죽어 있음). 그 상태로 플러그를 재기동해 **사용자의 Windows 세션을 하드 전원차단시킴**. 플러그 전력(DPS 19 ÷10 = W)은 "뭔가 켜져 있다"만 알려줄 뿐 **어느 OS인지 구분하지 못한다** — 전력이 높으면 재기동 금지하고 사용자에게 물을 것. 부수 사실: `server_up.sh`는 플러그가 이미 ON이면 `plug on`이 AC 전환을 못 만들어 no-op이라 180s를 흘려보내고 타임아웃한다. 진단·복구 절차는 `ubuntu-server` 스킬에 정리해 둠. 서버 렌더 실측: 12코어에서 48s·1440프레임 **25초**.
24. **아웃트로 "구독" 버튼을 호스트가 가리고 있었다**(2026-07-15, EP.6에서 발견 — **EP.5 렌더에도 있던 기존 결함**). `.subscribe`가 `left:0;right:0;margin:0 auto`로 중앙 정렬인데 host(x:30%, w:420)의 팔·머리가 첫 글자를 덮음. 채널 CTA라 치명적 → `right:80px` 우측 정렬로 수정. **풀폭 박스(`left:0;right:0`)에 scale 트윈을 걸면 back.out 오버슈트가 씬 밖으로 나가 `clipped_text` 오검출**도 난다(EP.6 s7 실측 3px) — `width:max-content; margin:0 auto`로 묶을 것(EP.5 `.subscribe`가 이미 쓰던 패턴).
25. **컷아웃 흉상은 성별을 못 버틴다 — rig가 통과하는 건 전적으로 치마 덕이다**(2026-07-15, EP.7). 5만원권 안에 신사임당 초상을 그리려다 **블라인드 검수 3연속 실패**: ① 쪽을 머리 옆에 떨어뜨림 → **헤드폰 이어컵** ② 머리채로 이어 붙임 → **헤드셋 붐마이크** ③ 머리로 얼굴을 감쌈 → 링이 턱 아래서 닫혀 **"수염 난 대머리 남자"**. 검수자 2명이 독립적으로 같은 진단을 냈다 — "같은 얼굴이 s5·s4에서 여성으로 읽히는 유일한 이유는 **치마**다. 흉상엔 치마가 없다." **머리 옆에 붙는 어두운 덩어리는 이 그림체에서 무조건 헤드폰 계열이 된다.** → 초상을 폐기하고 지폐엔 포도만 남긴 뒤, "이 사람"의 지시 대상은 **지폐 옆에 세운 lady rig 실물**이 맡게 해 해결(전신이라 치마가 살아남). **교훈: 인물 판독이 필요하면 흉상 그리지 말고 rig 전신을 세워라.**
26. **짚 술 달린 대칭 쌍이 입구를 협시하면 시메나와(일본 신사 금줄)가 된다**(2026-07-15, EP.7 S8 고증). 균분상속을 그리려 볏단 2단을 건물 좌우에 배치했더니 "시메나와/시데"로 지적됨 — 한국의 **금줄은 입구를 가로질러 치지, 좌우 한 쌍으로 세우지 않는다.** 블라인드도 같은 소품을 "명명 불가·공중부양"으로 불합격(벌집 skep → 빗자루 → 서핑보드). → **좌우 대칭 배치 자체를 버리고** 장독대를 좌측 단독으로. **소품 좌우 대칭은 그 자체가 의미(제의·신사)를 만든다 — '똑같음'을 보여주려고 대칭을 쓰면 다른 뜻이 붙는다.**
27. **소품이 사실 오류일 수 있다 — 고증 게이트는 미술이 아니라 사료를 본다**(2026-07-15, EP.7). "재산도 딸 아들 똑같이"를 엽전 꾸러미로 그렸는데 S8이 **시대착오**로 적발: 조선 전기는 **화폐 유통에 실패**해(조선통보 1423 정착 실패) 통화 역할은 **쌀과 베**였고, 엽전이 부의 상징이 되는 건 **상평통보(1678~)** 이후 — 사임당(1504~1551)보다 130년 뒤다. facts.md에도 균분 대상이 '노비·토지·주택'이라 적혀 있었는데 그림이 사료를 앞질렀다. **교훈: S6 저작 시 소품을 정할 때 facts.md를 다시 볼 것. 화면에 들어가는 물건도 팩트체크 대상이다.**
28. **재사용 자산도 고증은 재검수 대상**(2026-07-15, EP.7). bench sp8 동헌(haiku 명명 통과)을 처가로 복사했더니 S8 고증이 3건 지적 — ① **단청(붉은) 기둥은 궁궐·사찰 전용**, 사가엔 민흘림 나무 기둥 ② **정격자 창 = 일본 쇼지**, 한국 창호는 **세살/띠살**(촘촘한 세로살 + 가로살은 띠로 몰아서) ③ **기왓골이 가로 줄무늬** = 일본 지붕 문법, 한국 기왓골은 **경사면을 따라 세로**. 관청(동헌)에선 단청이 맞았으니 자산 자체는 틀리지 않았다 — **용도가 바뀌면 고증도 바뀐다.** 수정 후 재검수 전량 correct.
29. **실물 유물은 원래 형식대로 마운트할 것**(2026-07-15, EP.7). 안견 「몽유도원도」를 세로 족자에 걸었더니 S8이 지적 — **몽유도원도는 족자가 아니라 횡권(가로 두루마리)**다. 축을 좌우로 옮겨 가로로 펼쳐 마운트해 해결. 블라인드는 "같은 영상에서 족자(상하 축)와 두루마리(좌우 축)가 섞여 오류로 보인다"고 했지만 **고증이 우선** — 실제로 다른 형식의 물건이다. 실물 인서트를 쓸 땐 그 유물이 족자/횡권/화첩 중 무엇인지 확인할 것.
36. **길쭉한 원통 + 나팔형 입구 = 대포/망원경으로 적극 오독된다 — 청동 기물은 짧고 굵게 + 개방 윗면**(2026-07-19, EP.12). sp4 측우기 원본(몸통 세로:가로 ≈ 3:1, 상단 림이 몸통보다 넓게 벌어진 나팔형)을 그대로 썼더니 **블라인드·고증 검수가 동시에 "대포/포신/망원경/굴뚝"으로 적극 오독**(블라인드 Top1, 고증 최우선). 실물 측우기는 짧고 뭉툭(높이:지름 ≈ 2:1)한 원통이 화강암 측우대 위에 놓인 형태. → **원통을 짧고 굵게(≈1.5:1) 재작화 + 상단 림을 몸통과 같은 폭의 평평한 개방면(어두운 내부 타원 = "위가 뚫린 용기")으로 + 나팔 벌어짐 제거 + 밴드는 얇은 이음선으로**(굵은 밴드=드럼/대포 링). 재작화 후 블라인드 3/3 "위가 뚫린 원통 용기"·"대포 아님"으로 통과. **교훈: 지식 의존 기물(측우기·자격루류)은 '스탬프 동반'만으로 부족 — 실루엣이 다른 물체로 *적극* 오독되면(용기→대포) 라벨이 있어도 혼란. 실루엣부터 '용기'로 읽히게 만들고 라벨은 정체성 확정용.** 청동 원통 세로비는 항상 짧게, 개방 용기는 어두운 내부를 보여줄 것. props-catalog 측우기 항목 갱신.
30. **결각·톱니는 작은 크기에서 화살표가 된다**(2026-07-15, EP.7). 포도 잎을 "실물처럼 결각지게" 다각형으로 그렸더니 화면 40px에서 **초록 화살표/지그재그**로 읽힘. → **둥근 잎 덩어리 + 잎맥 스트로크**로 되돌리니 "잎" 판독. 소품 디테일은 렌더 크기에서 검증할 것 — 실물에 가깝다고 더 잘 읽히는 게 아니다.

31. **자체 팩트체크는 facts.md의 오류를 못 잡는다 — S3에 사료를 새로 여는 적대적 검증자를 붙여라**(2026-07-16, EP.8). S1 리서치가 「此實天幸」을 "난중일기 9.16의 **마지막 문장**"이라고 보고했고, S3 자체 대조는 대본↔facts.md만 보므로 그대로 통과시켰다. **적대적 검증(고증 악플러 롤)이 원문을 새로 열어 적발** — 天幸 뒤에 "물이 빠져 진을 옮겼다가 달빛을 타고 당사도로 이동해 밤을 지냈다"가 이어지고 **일기는 그 이진 서술로 끝난다**. 위험도가 최대였던 이유: ① 원문 한 줄로 즉시 반증(해석·판본 핑계 불가) ② 하필 **펀치라인** ③ 없어도 되는 수식이었음. **해결이 오히려 개선**: 위치 주장을 빼고 실제 원문 순서(천행 → 물때 → 이진 = 끝)를 펀치라인으로 승격 → "세계 해전사의 기적을 네 글자로 자평하고 물때 얘기로 끝낸 무심함"이 더 강한 훅이 됐다. **S3 게이트에 적대적 검증자 1명을 상설화할 것.**
32. **TTS 오발음은 실단어로 붕괴할 때가 진짜 위험 — A/B 2~3문맥으로 판별**(2026-07-16, EP.8). edge-tts `ko-KR-InJoonNeural` 실측 재현: **"부하"→"부활"(3/3)**, **"안위(야/가/를)"→"아니(야/가/를)"(3/3)**, "칠천량"→"7천양/7000양"(2/2), "딱 네"→"딱 내"(2/2). "안위야, 군법에 죽고 싶으냐"가 **"아니야, 군법에…"**로 뒤집혀 명대사가 통째로 파괴됨. **회피법**: 부하→**장수들**(clean), 안위야→**이름을 빼고 원문 더 길게 인용**("군법에 죽고 싶으냐")하고 이름은 스탬프로, 칠천량→**"한 번의 패전"**으로 우회하고 이름은 스탬프로(EP.2 금오 플레이북). **"네→내"는 whisper 아티팩트라 허용**(문맥상 무중의, `--model medium`이면 정상 전사). 지뢰 #9의 후속 사례 — 고유명사·2음절 한자어는 무조건 A/B.
33. **whisper `--model small`이 마지막 줄을 통째로 누락한다**(2026-07-16, EP.8). 60초 나레이션에서 아웃트로("삼십 초 역사… 구독") 6단어가 전사 결과에 없고, 직전 단어 "끝이었습니다"의 end를 59.7s까지 늘려 덮었다. 오디오엔 멀쩡히 있었다(구간 잘라 재전사해 확인). `sync_timing.py`가 이 타이밍으로 자막을 만들면 아웃트로 자막이 붕괴한다. → **`--model medium` 사용**(누락 복구 + 네/내까지 정상). 스킬의 `--model small`은 하한선이지 안전선이 아니다. **검증법: 전사 마지막 단어가 script.txt 마지막 어절과 일치하는지 매번 대조.**
34. ~~**edge-tts는 줄바꿈마다 ~1.5~2.3s 묵음을 넣는다**~~ → **정정(2026-07-21, EP.14 실측): 묵음을 만드는 건 줄바꿈이 아니라 마침표다.** 8줄 대본에서 두 줄을 한 줄로 합쳐 봤더니 길이가 **52.416s → 52.416s로 정확히 0초** 변했다(어절·문장 수 불변, 줄 수만 −1). 즉 줄바꿈 자체의 비용은 0이고, EP.13이 찾은 "문장 수가 지배 변수"가 맞다. 실측 모델: 문장 경계당 **약 1.8s** + 어절당 약 0.4s. **길이를 줄이려면 줄을 합치지 말고 문장을 합치거나 어절을 깎아야 한다.** (EP.14는 7행의 두 문장을 한 문장으로 합쳐 52.4→50.7s.) 원문 기록: (2026-07-16, EP.8). 9줄 대본에서 묵음만 ~14s로, 어절 수(≈2.5어절/s) 기반 추정이 크게 빗나간다(예상 43s → 실측 68s). 줄 수를 줄이거나 문장을 합치는 게 어절을 깎는 것보다 효과가 크다. **길이 드리프트 경고**: EP.5 44.6s → EP.6 48.0s → EP.7 53.3s → **EP.8 59.1s**. 브랜드가 "삼십초 역사"인데 2배다 — 다음 편은 씬 7개·대본 7줄 이내로 되돌릴 것.

35. **실물 이미지를 '새김' 오브젝트에 얹으면 그 오브젝트로 안 읽힌다 — 구조적 충돌**(2026-07-17, EP.9). 대동여지도 **목판**(인쇄용 나무판)에 실물 대동여지도 이미지를 얹었더니 블라인드가 3연속 "나무 상자/궤짝·액자 속 지도"로 판독, "목판"으로는 0. 원인: 인쇄 목판은 표면에 그림이 **음각으로 새겨진** 것인데, 깨끗한 컬러 사진을 나무 위에 얹으면 필연적으로 "나무에 걸린/든 지도"가 된다. 시도한 것: 광곽+격자+제목칸(→"환풍구"), 마구리 끝단목(→"상자 뚜껑"), `mix-blend-mode:multiply`로 나무결에 융합(→여전히 "종이 얹힌"). **교훈: 유물 실물 이미지는 그 유물이 '평면 전시물'(지도·그림·문서)일 때만 컷아웃 프레임에 넣어 쓴다. '표면에 상이 새겨진 물체'(목판·비석·전각)는 실물 이미지가 아니라 그린 새김으로 표현해야 그 물체로 읽힌다.** EP.9는 사용자가 "실물 이미지 우선"을 택해 목판 장면(s1·s6)은 '유물을 나무에 전시'한 판독으로 수용(자막 '목판'이 명칭 보강). s3(대형 지도)·s2(서안 위 지도)는 평면 지도라 실물 이미지가 완벽히 맞음. **다음에 목판·비석류가 나오면 실물 이미지 금지, 그린 새김(EP.9 S6.4의 광곽+한반도 음각 버전이 "새긴 지도판"으로 통과했던 것)으로.**

37. **CSS flip은 옷 여밈(우임)을 반전시킨다 — 교차 깃 의상은 flip-인지형으로**(2026-07-19, EP.13 S8 고증). militia 도포의 깃이 원래 좌표 기준 좌임(左衽 — 수의/오랑캐 문법, 사극 악플 단골)이었는데, 역대 에피소드에선 militia가 대부분 `flip:true`(CSS scaleX -1)라 화면상 우임으로 보정돼 12편 동안 아무도 못 잡았다. EP.13 이우가 첫 unflip 도포 노출 → 즉시 적발. → `characters.js` 도포 깃/옷고름을 `cfg.flip` 조건 분기(unflip=거울 좌표로 우임, flip=원래 좌표가 미러 후 우임). **잔여**: lady·kid 저고리 동정도 같은 구조(bright 외깃이 화면 왼쪽 어깨 출발) — lady/kid를 unflip으로 쓰는 씬이 나오면 같은 분기 적용할 것.

38. **서브에이전트가 보고한 실록 기사 ID·한문 원문은 지어질 수 있다 — 인용은 반드시 직접 열어 대조하라**(2026-07-21, EP.14). S1 검증 에이전트가 세종 3년 착호갑사 기사를 `kda_10303014_001` 「兵曹啓請定捉虎甲士二十名。上王依施。」로 보고했는데 **기사 ID도 원문도 실재하지 않았다**(`_001`은 종묘 망제 기사). 실제는 `kda_10303014_004` 「捉虎甲士當下番, 皆定爲二十…上王從之」로 내용이 상당히 다르고, 그 차이가 대본을 바꿨다(창설 기사가 아니라 증원 기사, 재가 주체는 상왕 태종). 적대적 검증자가 적발했다. **한문은 그럴듯하게 생성되기 쉬우므로, 대본·스탬프에 들어가는 원문은 예외 없이 sillok.history.go.kr 페이지를 직접 fetch해 대조할 것.**
39. **역방향도 있다 — 검증자의 "사료에 없음" 판정도 검증하라**(2026-07-21, EP.14). 적대적 검증자가 "영조 30년 경기 한 달 120여 명"을 **❌ 실록에서 확인 안 됨**으로 판정했으나 오판이었다. 원인: 검증자가 **기사 제목 색인**을 훑었는데 그 기사의 제목은 「효자 서태금·한수재의 여문에 정표하라고 명하다」라 호환 수치가 **본문에만** 있었다. 국역(`kua_13004119_002`)·원문(`wua_13004119_002`) 페이지를 직접 열어 「時畿甸虎患甚, 一朔內噉死一百二十餘人」 실재를 확인하고 기각. **제목 색인 기반 "없음" 판정은 부재의 근거가 되지 못한다 — 실록 검증은 본문을 열어야 한다.**
40. **세로 캔버스에서 소품 진입 x슬라이드 금지 — EP.1 규칙인데 되밟았다**(2026-07-21, EP.14). 호랑이 진입을 `x:160→0`으로 넣었더니 트윈 중간 프레임에서 소품이 캔버스 밖으로 나가 씬 scrollWidth가 부풀고 **`clipped_text` 에러**가 났다(t=26.1s 101px). 인스펙터가 에러 텍스트로 **엉뚱한 스탬프 문구**를 인용해서 원인 추적이 한 번 헛돌았다 — clipped_text의 인용 문구는 범인이 아니라 그 씬의 텍스트일 뿐이다. **캐릭터뿐 아니라 소품도 y슬라이드로.** 정적 폭도 `left+width ≤ 1080`을 저작 시 확인할 것.
41. **회전으로 쓰러뜨리면 머리가 프레임 밖으로 나가 '잘린 얼굴'이 된다**(2026-07-21, EP.14). 갑사를 `rotation:-72` (transformOrigin 50% 100%)로 쓰러뜨렸더니 머리가 화면 좌측 밖으로 밀려 **잘린 얼굴 + 자막 pill에 가린 몸통**만 남아 시신 클로즈업으로 읽혔다(톤·가독성 동시 실패). → **−20~−25° 비틀거림 + 소품 떨어뜨리기 + 충격선 + 암전**으로 충분히 "당했다"가 전달된다. 큰 각도 회전은 rig 높이(w×1.2)만큼 상단이 옆으로 쓸려나간다는 걸 계산에 넣을 것. 곁들여: **떨어뜨린 소품은 캐릭터가 항상 위에 렌더되므로 캐릭터 실루엣 밖에 착지시켜야 보인다**(1차 시도가 몸 뒤에 숨어 사라짐).
43. **정면 평면 원반 + 채 = 징(gong)이다. 북으로 읽히려면 원통 북통(배럴)이 보여야 한다**(2026-07-22, EP.15 S8). 신문고를 sp9 독전고 문법(정면 원형 북면 + 붉은 테 + 못 + 사선 북채)으로 그렸더니 블라인드가 **"북이라기엔 원통 몸통이 안 보이는 얇은 원반 → 징/공"**으로 판독. 하필 이 편은 **북(신문고) vs 징(격쟁)이 서사의 페이오프**라, 신문고가 징으로 읽히면 대비가 통째로 붕괴하고 s5의 진짜 징과 혼동된다(같은 영상 안에 두 타악기가 나오는 첫 사례). → **3/4 배럴로 재작화**: 앞 북면(타원)은 좌측 정면, **원통 몸통이 우측 뒤로 이어지고 반대쪽 북면 캡까지 살짝 노출** + **테hoop 2개(쇠테 — 드럼 시그니처)** + 널(stave) 결 + 2톤 셰이드. 재검 "북 — 확신 상, 징 오독 여지 낮음". **교훈: 타악기는 실루엣 두께가 정체성이다. 원반=징, 원통=북.** 덤으로 **머리 위 원형 기물 금지 재확인** — s5 징을 든 팔 위에 뒀더니 "놋대야 뒤집어쓴 대머리"로 오독, 머리 옆으로 떼자 "손에 든 징"으로 정상 판독(지뢰 #25·S6.4 #4 계열).
44. **inspect 샘플이 씬 전환창에 떨어지면 clipped_text 오검출이 난다 — 범인은 슬라이드 중인 stage다**(2026-07-22, EP.15). `npm run check`(핀 0.7.31)가 s2에서 `clipped_text #s2 overflowed right 120px`를 스텝 텍스트 문구와 함께 계속 뱉었는데, 그 텍스트는 전부 left≤130의 정적 요소라 기하학적으로 넘칠 수가 없었다. JSON(`inspect --json`)을 까 보니 `rect:{left:120.36, right:1200.36, width:1080}` — **#s2의 rect가 통째로 오른쪽으로 120px 밀려 있었다 = 진입 x슬라이드 중인 `.stage`**(지뢰 #40의 "인용 문구는 범인이 아니다"가 씬 레벨에서 재현). 인스펙터 기본 샘플은 `duration*(k+0.5)/9`(44s면 2.44/7.33/12.22…)라 **s2 전환창(7.11~7.56)에 7.333이 정확히 걸린 것뿐**이고, EP.14가 통과한 건 샘플이 전부 전환 뒤에 떨어진 운이었다. → **해당 씬의 stage 슬라이드만 0.45→0.2s로 줄여 샘플 전에 정착**시켜 해결(씬 시작·오디오 싱크 무손실). **진단법: 에러 문구를 믿지 말고 `inspect --json`의 `rect`를 볼 것. rect가 캔버스 폭 그대로인데 left가 밀려 있으면 전환 중 stage다.** 참고로 `npx hyperframes inspect`(최신 0.7.67)는 이 케이스를 안 잡으므로, 버전 차이로 "고쳤다" 착각하지 말 것 — 판정은 항상 핀 버전(`npm run check`)으로.
42. **스케일 교정은 과보정하기 쉽다 — 두 번 재는 게 싸다**(2026-07-21, EP.14). 검수가 "호랑이가 집보다 커서 판타지 괴수"라 지적해 호랑이를 680→470px로 줄였더니 이번엔 "담장 뒤 고양이"가 됐다. 570→520으로 두 번 더 조정해 착지. **스케일 지적을 받으면 한 번에 목표치로 가지 말고, 기준 오브젝트(집·캐릭터) 대비 비율을 숫자로 정한 뒤(호랑이 ≈ 집 폭의 0.8) 재캡처로 확인할 것.** 깊이는 크기만이 아니라 **지면 접점 y**로도 만든다(전경 소품의 발이 중경 담장 밑동보다 아래에 와야 앞으로 읽힘).

45. **밤 실내의 세살 창은 명암 방향이 뒤집히면 감옥 창살이 된다**(2026-07-23, EP.16 S8). 창 안에 밤하늘+달을 넣어 '밤 실내'를 한 소품으로 성립시키려 했더니, **어두운 면 위의 밝은 세로살**이 되어 블라인드가 확신도 '상'으로 **"감옥 창살/우물 격자"** 판독. 검수자가 "배경에 달과 밤하늘이 있어 감금 시설처럼"이라고 원인까지 짚었다. 실제 조선 창은 **한지를 발라 밝은 면 + 어두운 살**이고 **하단에 궁판(널)**이 있다 — 즉 고증대로 그리면 오독도 사라진다(한지 창으론 달이 안 보이는 게 맞다). → 창호지 면 + 어두운 살 + 가로살 2단 분산 + 궁판으로 재작화해 "창문/동아시아 고건축 목창"으로 통과. **교훈: 시그니처 표의 형태 항목만 지키고 명암 방향을 안 보면 같은 형태가 정반대 물건이 된다.**
46. **세로쓰기 글줄은 '선'이 아니라 '글자의 연속'으로 그려야 한다**(2026-07-23, EP.16 S8). 문서·편지·상소 지면의 글줄을 **긴 통막대(rect) + 가로 눈금**으로 그렸더니 눈금이 막대와 만나 **십자(+)**를 만들어, 블라인드가 확신도 '상'으로 **"십자가 무늬 기록판/격자 기록판"** 판독. EP.8·EP.10에서도 같은 방식을 썼는데 그땐 지면이 작아 안 걸렸고, **이 편은 편지가 주제라 지면을 크게 키운 순간 드러났다**. → **짧은 글자 획(높이 12~26px)을 간격 7~13px로 세로로 끊어 쌓는 방식**으로 전량 재작화(편지 3종·상소·두루마리 5곳) → "문서/기록부" 판독. **교훈: 작아서 안 걸리던 결함은 크게 쓰는 순간 터진다 — 자산을 확대 재사용할 때 시그니처를 다시 볼 것.**
47. **배경 글로우도 소품과 같은 실루엣 규칙을 받는다 — 큰 원은 달이 된다**(2026-07-23, EP.16 S8). 씬 분위기용으로 깐 반투명 원형 글로우(opacity 0.10)를 블라인드가 확신도 '상'으로 **"큰 달(다크 그레이 원)"**이라 명명하고, 그걸 그 씬의 주요 오브젝트로 취급했다. 시그니처 표의 "원형 기물 단독 클로즈업 = 행성/달" 항목을 **배경 장식에서 되밟은 것** — 표를 소품에만 적용하고 배경 레이어는 면제로 취급한 게 원인. → **가로로 넓은 저채도 타원 띠**로 교체해 해소. **교훈: 오독 규칙은 '소품'이 아니라 '화면에 보이는 모든 형태'에 적용된다.**
48. **어찰첩처럼 '나중에 채워지는' 소품은 용기와 내용물을 별도 prop으로 분리하라**(2026-07-23, EP.16). 어찰첩 6첩을 씬 후반(37.6s)에 통째로 등장시켰더니 그전 6초 동안 **화면 상단 절반이 단색으로 비었다**(S6.4 #6). 선반장(케이스)과 첩을 별 prop으로 쪼개 케이스는 씬 시작부터 노출하고 첩만 나중에 채우니, 공백이 사라지는 동시에 **"빈 선반 → 편지로 채워짐"**이라는 서사까지 얻었다. 부수 효과로 첩이 케이스 안에 앉아 **부양 문제(S6.4 #8)도 같이 해결**됐다 — 얇은 받침만 있던 초안은 공중에 떠 있었다.
49. **벽 밴드는 씬 stage의 첫 요소로 넣어야 한다**(2026-07-23, EP.16). 창·글로우가 허공에 뜨는 걸 막으려고 벽 밴드 div를 추가하면서 **DOM상 기존 소품 뒤에 삽입**했더니, 풀스크린 벽이 창·글로우를 통째로 덮어 3개 씬에서 소품이 사라졌다. 인스펙터는 0 에러(요소는 존재하니까), `npm run check`도 통과 — **스냅샷을 안 봤으면 그대로 렌더될 뻔했다.** S6.4 씬별 루프가 잡은 케이스. 풀블리드 배경 요소는 항상 stage 최상단(DOM 최선두)에 둘 것.

50. **빈 어좌는 "붉은 문짝/걸어놓은 북"이 된다 — 앉는 물건은 사람이 앉아야 자리로 읽힌다**(2026-07-24, EP.17 S8). EP.5→EP.10→EP.16을 거쳐 네 번 통과한 어좌 자산인데, 이번 편은 **왕이 비어 있는 어전**이라 처음으로 단독 노출됐고 블라인드가 **확신도 '하'로 "붉은 판+금테+곡선 지붕 = 문짝 / 북걸이에 걸린 북 / 감실"** 판독했다. "등받이·팔걸이·방석이 없다"는 게 검수자가 댄 이유. **자산이 통과했던 건 늘 왕이 그 위에 있었기 때문**이지 어좌 자체가 읽혔던 게 아니다(EP.14 호랑이 민화·EP.15 독전고 계보 — **재사용 자산의 통과 이력은 그 자산이 단독으로도 읽힌다는 뜻이 아니다**). → 좌판에 대비색 방석을 얹고, **순조(11세)를 king rig w280으로 어좌 앞에 세워** 해결. 고증상으로도 순조는 그 자리에 있었다(「上曰: …依爲之」) — **가독성 수정이 사료 충실도를 같이 올린 사례.**
51. **반투명 군중 실루엣은 유령이 된다**(2026-07-24, EP.17 S8). 대신 열을 opacity 0.72로 깔았더니 블라인드가 **"희미하게 사라져 가는 관리들 / 유령·혼령 / 페이드 중인 잔상"**으로 판독하고, 그걸 그 씬의 서사(무언가 사라지는 중)로 해석했다. 원경 표현으로 쓴 투명도가 **의미를 만들어 버린 것.** → 0.95↑ 불투명 + 중경 스케일(주인공 높이의 55%↑)로 해결. **깊이는 투명도가 아니라 크기·지면 접점 y로 만들 것**(지뢰 #42 계열).
52. **세로 막대가 배경 구조물을 관통하면 z-순서 조정만으로는 안 끝난다 — 길이를 잘라라**(2026-07-24, EP.17 S8). 깃대 3개가 성벽·문루를 뚫고 지면까지 내려와 블라인드가 "깊이가 무너져 **반투명하게 겹친 렌더 오류**처럼 보인다"고 지적. DOM에서 깃발 블록을 성곽보다 앞으로 옮겼는데도 렌더 결과가 그대로였다(원인 미규명). → **깃대 rect의 height를 잘라 성벽 뒤에서 끝나게** 해서 확정 해결. 가림 문제는 그리기 순서보다 **기하로 푸는 쪽이 결정적**이다.
53. **`hyperframes transcribe`(whisper.cpp)는 한글을 U+FFFD로 깨뜨리고 마지막 문장을 뭉갠다 — faster-whisper로 우회**(2026-07-24, EP.17). 두 결함이 동시에: ① 음절이 토큰 경계에서 쪼개져 `transcript.json`에 **�**가 박힌다(EP.15 커밋본에도 있음 — 지뢰 #5가 "터미널 인코딩 문제일 수 있음"으로 기록한 게 사실은 **파일 자체의 손상**이었다. 그동안 sync_timing이 script.txt 기준 문자 정렬로 흡수해 와서 안 드러났을 뿐이고, **S4 귀검증은 그 손상된 텍스트로는 아예 불가능**했다) ② 47.8s 오디오를 43.0s로 끊고 아웃트로 7단어를 42.74~43.00에 몰아넣었다(지뢰 #33 재발, `--model medium`인데도). → `uv run --with faster-whisper`로 **word_timestamps=True, vad_filter=False** 전사해 transcript.json을 직접 생성(스킬 S4에 코드 박제). vad_filter를 켜면 아웃트로 세그먼트가 떨어진다. **덤: 깨끗한 전사가 생기자 A/B 판정이 뒤집혔다** — hyperframes 전사로는 "장용영→장용형·신하→신화"가 붕괴로 보였는데 faster-whisper로는 **장용영·군영이 정확**했고 진짜 붕괴는 "태우라→태어라"였다. **엔진 아티팩트를 TTS 오발음으로 오진할 뻔했다.**

16. **모델은 지침의 하한선까지만 그린다**: "구성 요소 3~6개 목록화"만 주면 요소당 프리미티브 1개짜리 막대그림(깃대 rect+깃발 polygon+창날 polygon = 끝)이 나옴 — EP.4 sonnet5 세션(2026-07-12)에서 "배경 단색+미니 깃발 2개+캐릭터" 씬, 남색 배경에 암갈 단색 슬랩 건물(명암·대비 없음), 소품 0개 씬("제주도 유배"에 해+바다 띠뿐)이 실제로 나옴. → 스킬 S6에 **씬 구성 최소 규격**(3레이어 / 주 소품 폭 ≥380px / 프리미티브 ≥12·그룹 ≥4 / 2톤 명암 / 배경 명도 대비)과 **소품 라이브러리 복사 우선** 규칙을 숫자 기준으로 명문화, S6.4 체크리스트에 재검 항목 추가. 지침은 세기 가능해야 모델 무관하게 지켜진다.

## 도구: 레이아웃 튜너 (studio-layout.html) — 2026-07-10 추가

`studio.html`(캐릭터 얼굴 파츠 튜너)에 이어 두 번째 자체 GUI. **캐릭터·배경·소품의 위치(x·y)·크기를 드래그/슬라이더로 조정** → 비파괴 오버라이드 파일 `layout-overrides.js` 생성.

**2026-07-11 파이프라인 공식화**: LLM이 찍는 배치가 매번 어긋난다는 사용자 결론에 따라, `/episode` 스킬에 **S6.5 "인간 레이아웃 조정"** 단계로 편입 — 렌더 전에 반드시 사용자가 튜너로 조정하고, 조정값은 Claude in Chrome이 자동 회수한다(아래 "조정값 회수" 참조). 에피소드 시작 시 `layout-overrides.js`는 빈 오버라이드로 리셋, S9에서 아카이브에 포함.

- **아키텍처**: `char-tuning.js`와 동일한 오버라이드 패턴. index.html이 `layout-overrides.js`(전역 `LAYOUT_OVERRIDES`)를 읽어 각 요소에 적용. 비어 있으면 원본 그대로(렌더 무영향).
- **적용 방식**: 대상 요소를 stage를 꽉 채우는 `.adj-wrap`으로 감싸고 wrapper에 `transform: translate(dx,dy) scale(s)` + `transform-origin: ox oy`(요소 홈 중심) 적용. **GSAP 진입 트윈은 내부 요소(선택자 그대로 매칭)를 애니메이트하므로 wrapper 오버라이드와 충돌하지 않음** — 정적 오버라이드를 요소 자신의 inline transform으로 주면 GSAP가 덮어써서 사라짐(그래서 wrapper 필수).
- **scale 오버라이드 → 씬 overflow 풀기(2026-07-10)**: `.adj-wrap`이 stage 전체(1080×1920)라 큰 `scale`(예: 1.81)을 주면 wrapper 렌더 박스가 stage 밖으로 커지고, 부모 씬(`.scene`은 `overflow:hidden`)의 `scrollWidth`가 부풀어 **`clipped_text` 오검출**이 난다(실제 텍스트는 안 잘림 — 투명 빈 공간일 뿐). `data-layout-allow-overflow`는 `text_box_overflow`만 억제하고 `clipped_text`엔 안 먹힘(인스펙터 룰이 `clipsOverflow`인 조상만 검사). → `applyAdjust`에서 오버라이드(scale≠1 또는 translate)가 걸린 **해당 씬만 `overflow:visible`**로 풀어줌. 스테이지 밖은 1080×1920 렌더 캔버스에 안 잡히므로 시각 영향 0. (SVG 소품은 `offset*`가 없어 자식 박스 측정 기반 shrink-wrap은 불가 → 이 방식이 최선.)
- **조정 대상 등록**: 캐릭터는 마운트 시 `data-adj=<char id>` 자동 부여. 배경/소품은 **저작 시점에 `data-adj="<씬id>-<이름>"`을 직접 부여**하는 것이 규칙(2026-07-11부터 — index.html은 에피소드마다 재작성되므로 저작 규칙이 런타임 맵보다 단순). 김시습 편 index.html의 `ADJ_MAP`(선택자→키 런타임 등록)은 구방식 유산으로 그대로 동작하지만 신규 저작에는 쓰지 않는다. 부위 분할 입도: 논리 부위(층·지붕·몸체)뿐 아니라 시각적으로 독립된 세부 조각(몸돌·지붕돌·상륜)까지 `<g data-obj>` 중첩 그룹핑 — 그룹핑 입도 = 조정 입도.
- **조정값 회수 (2026-07-11 변경)**: 다운로드 버튼 → 수동 복사 대신, 사용자 완료 신호 후 Claude in Chrome `javascript_tool`로 `__layout.get()` + `__layout.getObjects()`를 JSON으로 받아 `layout-overrides.js`를 로컬에서 조립·저장(에디터와 iframe이 같은 3014 origin이라 접근 가능). **주의: `overridesText()` 원문 문자열 반환은 Chrome 확장의 데이터 유출 가드가 차단**(2026-07-11 실측 — JSON 객체 반환은 통과). 탭이 닫혀도 localStorage(`toon-layout-overrides-v2`) 자동저장분이 재오픈 시 복원됨. 다운로드/클립보드 버튼은 수동 폴백으로 유지. 왕복 검증 완료: 파일 저장 → index.html 하드리로드 → `.adj-wrap` transform 반영 확인.
- **편집기 훅**: index.html이 `window.__layout`(list/get/set/rectOf/measureOrigin/overridesText) 노출. 편집기는 iframe으로 index.html을 로드하고 `window.__timelines.main.pause(t)`로 씬 이동 후 훅으로 요소 박스를 그림.
- **씬 이동 타임**: 각 씬을 `start + dur - 0.7`로 seek — 진입 트윈이 끝나고 **늦게 등장하는 요소(스탬프·숫자 타이포)도 이미 보이는** 시점. 다음 씬 진입 전이라 겹침도 없음.
- **실행 전제**: 정적 서버로 열어야 iframe이 index.html + 상대경로 스크립트를 로드함. `.claude/launch.json`의 `toon-pilot-static`(python http.server 3014) → `http://localhost:3014/studio-layout.html`. (hyperframes preview 서버 3013이 아님.)
- **좌표 변환**: 편집기 iframe은 `transform: scale(0.4)`로 축소 표시 → 드래그 화면delta / 0.4 = 컴포지션 px. 요소 rect은 iframe 내부 문서 기준(1080-base)이라 축소 무관.
- **회전 조정(2026-07-24 추가, EP.18)**: 튜너에 `회전` 슬라이더(−180~180°) 신설. 오버라이드 스키마에 `r`(도) 추가 — 오브젝트(`applyObject`)는 `rotate(r cx cy)`(부위 bbox 중심 기준), 요소·캐릭터(`applyAdjust`)는 CSS `rotate(rdeg)`(요소 홈 중심). `setInputs`/이벤트 리스너(rr/nr)로 노출, 회수 JSON에 자동 포함. EP.18 강보 아기 머리(s1/s3/s5-baby-head)를 −71~−75°로 눕혀 자연스러운 신생아 각도 구현.
- **소품 내부 부위 조정(2026-07-10 추가)**: 소품(석탑·기와집 등)의 논리적 부위를 `<g data-obj="키">`로 묶고(pagoda-base/tier1/tier2/tier3/finial, palace-roof/body/podium, hall-roof/body, hermitage-roof/body), 편집기에서 요소 선택 시 그 하위 `[data-obj]`를 "부위"로 드릴다운. 부위 오프셋은 **SVG `<g>`의 `transform` 속성**으로 적용(`OBJECT_OVERRIDES`) — 요소 오프셋(CSS wrapper transform)과 별개 레이어. 크기 조정은 `translate(cx cy) scale(s) translate(-cx -cy)`로 부위 bbox 중심 기준(`getBBox()`로 cx/cy 계산·저장). 드래그 화면델타→SVG단위 변환은 `g.parentNode.getScreenCTM().inverse()`로(iframe 축소·부모 스케일 모두 자동 반영). 새 부위를 조정 대상에 넣으려면 해당 요소 안에서 primitive들을 `<g data-obj>`로 감싸면 끝(자동으로 편집기에 노출). GSAP가 개별 애니메이트하는 자식(크라운 g·불꽃 flame-inner)은 부위로 묶지 말 것(정적 transform과 충돌).

## 숫자 타이포그래피 규칙 (2026-07-10 사용자 피드백)

"5살/3일을 숫자로 표시"는 **자막 텍스트를 숫자로 바꾸는 게 아니라**, 자막은 자연 표기("다섯 살"·"사흘")로 두고 **별도의 큰 숫자 타이포("5살"·"3일")를 화면에 팝**시키라는 뜻이었음. `.num-pop`(Black Han Sans, 숫자만 금색 강조) + scale 0→1 팝 애니메이션. 숫자 타이포는 배경 그래픽(크라운 등)과 겹치지 않게 빈 구역에 배치 — s3에서 처음엔 크라운과 겹쳐 재배치(중앙→좌측). **교훈: 자막(내레이션 싱크)과 강조 타이포(시각적 훅)는 별개 레이어다.**

## 룩 이슈 (2026-07-05 사용자 피드백 → 같은 날 전부 수정, v3 렌더 반영)

전 항목 수정 후 블라인드 readability 테스트 통과(무맥락 에이전트 4개가 지도·한옥·갓·갑주·거북선을 high confidence로 명명). 수정 요지: 지도=실제 해안선 트레이스, 갓 챙 rx68>머리 r58, 익선관 날개=기운 매미날개 잎, 전립 챙 rx62 + 수염 말굽형, 호스트=네이비 칼라+고대비 지퍼+시보리, 한옥=기단·기둥·창살문·처마반전·용마루·기와골.

**⚠ 미해결(모자/갓) — 2026-07-08 부분 해결.** EP.1(장영실) 블라인드 검수에서 **전립(admiral)이 청 만주족 관모로 오독**됨(2/2 검수자, "돔+금구슬 정자+붉은 상모" = Qing 시그니처)이 확인돼 `characters.js` 수정: 전립=넓은 평챙+낮은 펠트 크라운+작은 증자(붉은술·금구슬 제거), 갓(militia)=높은 원통 크라운. 재검수서 "조선 흑립/전립(KOREAN)"으로 판독 = 청 오독 해소. **2026-07-08 후속: 참고이미지 3종 문제 재작화** — ① 익선관 소각(뿔)이 '토끼 귀'로 오독(둥근 만화 머리에선 위로 솟은 뿔=무조건 귀) → **뿔 제거, 머리 덮는 둥근 관+뒤 높은 이중단**으로 실루엣만 암시(세종 식별=곤룡포 담당) ② 갓이 서양 실크햇으로 오독(크라운 원통·과높음) → **낮은 절두원뿔 대우+넓은 양태** ③ admiral 수염이 검은 덩어리 → **얇은 밴드형·밝은 톤**. **교훈: 위로 솟는 모자 장식(뿔·깃)은 원형 캐릭터 머리에서 귀로 읽힌다 — 실루엣은 머리를 덮는 매스로 표현, 돌기는 피할 것.** 잔여 폴리시(비차단): 세종 관이 다소 단조(중절모 느낌), 거북선 용두 방향, s3 이순신 수면 위.

### 원래 이슈 표 (기록용)

| # | 증상 | 근본 원인 |
| --- | --- | --- |
| 1 | s2 한반도 지도가 뭉게구름처럼 보임 | index.html 지도 패스가 볼록 Q커브만의 프리핸드 블롭. 실제 해안선 특징(오목한 만, 서해안 요철, 남해 다도해)이 없음 — 실루엣을 실제 지도에서 단순화 트레이스해야 함 |
| 2 | 모자-머리 크기 불일치: 익선관 날개가 귀/뿔처럼 읽힘(s2 왕), 갓이 "이마 스티커"처럼 보임(s4) | 챙·관모가 머리 원(r58)보다 좁게 그려짐. **규칙: 챙 있는 모자는 머리 폭보다 넓게(갓 챙 rx≥66), 모자 하단이 머리 실루엣을 확실히 감싸고 겹치게.** 익선관 날개는 옆이 아니라 뒤통수 위쪽에 세워 배치 |
| 3 | 전립·턱수염 정합 어긋남 (s3 이순신) | 모자 챙 라인·수염 시작선이 얼굴 랜드마크(눈·볼 라인)와 독립적으로 좌표 지정됨 — 얼굴 기준선에서 파생하도록 |
| 4 | 호스트 의상이 뭘 입은 건지 안 읽힘 | 지퍼·포켓이 shirt색 20% 셰이드라 대비 부족, 칼라·소매 등 "재킷 문법" 셰이프 부재 |
| 5 | s1 기와집이 거북선으로 오인됨 | 지붕 곡면+어두운 색 덩어리가 몸체와 분리돼 읽힘. 처마 끝 반전, 기와 골 라인, 주춧돌 등 "건물 문법" 디테일 필요 |

**재발 방지 프로세스 (아트 작업 시 필수):**
1. **캐릭터 시트 검수** — 파츠 수정 후 풀프레임 축소본이 아니라 대형 스케일(캐릭터 1개 ≥600px) 스크린샷으로 변형/정합 확인. 임시 charsheet 페이지를 만들어 variant × 표정 × 포즈 그리드로 볼 것.
2. **블라인드 readability 테스트** — 렌더 프레임을 컨텍스트 없는 서브에이전트에 보여주고 "이 화면의 오브젝트를 명명하라"만 시킴. 의도한 이름(한반도, 기와집, 갓)과 다르게 답하면 불합격. 작화자가 자기 그림을 검증하면 의도를 알기에 오독을 못 잡는다 — 이번 이슈 전부가 이 케이스.
3. **실물 기반 오브젝트는 레퍼런스 트레이스** — 지도·건물·기물처럼 "정답 실루엣"이 있는 것은 프리핸드 금지, 실제 형태를 단순화해서 옮길 것.

## 다음 단계 (합의된 방향)

1. ~~**파츠 일러스트 교체**~~ ✅ 2026-07-05 완료 — rig 계약(클래스·피벗·표정/포즈) 유지한 채 characters.js만 교체: `shade()` 헬퍼(파라미터 색에서 2톤 플랫 셰이딩 파생), 유기 실루엣, 의상 고증(곤룡포 흉배·옥대, 두정갑 징·갑찰·전립 상모, 갓·동정·옷고름, 호스트 재킷). 얼굴 파츠(눈·입·눈썹)는 의도적으로 무수정.
2. **BGM/SFX** — hyperframes-media / media-use 스킬 (스탬프 슬램 효과음, 잔잔한 국악풍 BGM).
3. ~~**원커맨드 에피소드 스킬**~~ ✅ 2026-07-08 구현 — `.claude/skills/episode/SKILL.md` (주제 → 사료 리서치 → 팩트체크 게이트 → 대본 → TTS 귀검증 → `bin/sync_timing.py` 타이밍 동기화 → 씬 연출 → 렌더 → 블라인드 readability+고증 이중 검수 → `episodes/<slug>/` 아카이브+업로드 메타). 악플 3종(사실 오류·어눌함·고증)이 각각 게이트로 매핑됨. **✅ EP.1 장영실 완성**: `renders/janga-yeongsil.mp4`(1080×1920, 38.3s), 아카이브 `episodes/janga-yeongsil/`. 실전에서 게이트 작동 실증: ① 팩트체크가 측우기=문종(장영실 아님)을 대본에서 제외 ② 블라인드 검수가 전립 청-오독을 잡아 rig 수정 유발. **세로 재배치 규칙 실검증: 캐릭터 진입은 x슬라이드 금지→y슬라이드**(좁은 캔버스서 좌우 오버플로가 clipped_text 에러 유발). **✅ EP.2 김시습 완성**(2026-07-09): `renders/kim-siseup.mp4`(1080×1920, 41.2s), 아카이브 `episodes/kim-siseup/`. 캐스팅: 김시습=militia 고정(장영실과 달리 승진 없음, 색상만 베이지→회색 전환), 세종=king 변주. 게이트 실증: ① TTS가 "금오"를 일관 오발음해 대사에서 우회(경주 남산으로 치환, 금오신화는 화면 스탬프로만) ② 고증 검수가 무량사 탑의 빨강+금박 배색을 일본식 오중탑 오독 위험으로 지적해 무채색 화강암톤 재채색. 새 지뢰 2건은 위 "밟은 지뢰" #8·#9 참조. 다음: EP.3 주제 선정(episodes/janga-yeongsil/meta.md 후보 중 정약용·광해군·연산군 잔여). **✅ EP.3 정약용 완성**(2026-07-12): `renders/jeong-yakyong.mp4`(1080×1920, 43.5s), 아카이브 `episodes/jeong-yakyong/`. 게이트 실증: ① whisper가 "정약용"→"정야경" 표기 — TTS 오발음이 아닌 whisper의 '굥' 희귀음절 철자 편향으로 판정(A/B 대조 TTS + 모음 스펙트럼 분석, meta.md 참조) ② 신설 S6.4 프레임 자체 점검(스냅샷+5항목 기계 체크리스트)이 자막존 겹침 2건·스탬프-캐릭터 겹침·태양 후광 오독·색 동화를 렌더 전에 차단 → 블라인드/고증 검수 원패스. **✅ EP.4 광해군 완성**(2026-07-12): `renders/gwanghaegun.mp4`(1080×1920, 42.1s), 아카이브 `episodes/gwanghaegun/`. 이전 세션이 S6 최소 규격 도입 전에 만든 초안이라 s1(훅)·s6(제주 유배)·s7(여운)이 빈 화면이었던 것을 이번 세션에서 소품 신설(전고+화톳불, 초가+돌담, 빈 옥좌+일월오봉도+왕관)로 해결(위 "밟은 지뢰" #16 실사례). 별도 벤치(`bench/`, it1→it2, 78.3%→90.9%)로 소품 오독 방지 규칙을 먼저 검증한 뒤 실전 적용. 게이트 실증: S8 블라인드 검수가 s5 "닫힌 궁문"을 옷장/반닫이로 오독 — 벽과 문짝 폭을 맞추고 문고리·리벳을 더해도 여전히 옷장으로 오독(오히려 반닫이 문법과 일치해버림) → **"닫힌 문" 컨셉을 포기하고 창살(감금)로 전환해 해결**. 업로드: https://youtu.be/jmXDyLVjtmU (unlisted). **✅ EP.5 연산군 완성**(2026-07-13): `renders/yeonsangun.mp4`(1080×1920, 44.6s), 아카이브 `episodes/yeonsangun/`. 훅=일상어 어원("흥청망청")으로 폭군 잔혹사 톤을 사치·낭비 프레임으로 순화, 풀서클 펀치라인("이름은 지워졌지만 말은 남았다"). 소품 전량 라이브러리 재사용(술상 sp13·사약 sp2·국문장 sp7·어전 병풍 EP.4 s7·위리안치 sp3, 신규는 연등뿐) — 스프린트 자산이 제작 시간 대폭 단축. 게이트 실증: ① S8 이중 검수가 **lady rig 비녀→담뱃대 오독**(임란 이후 담배 = 조선 전기 시대착오)을 잡아 `characters.js` 비녀 제거(쪽+한복으로 여성 판독 충분 — 스프린트2 lady 쓰는 향후 에피소드 전부 반영) ② S6.5 튜너 localStorage 오염(아래 지뢰 #17). 업로드: https://youtu.be/jIYoZHxeLDo (unlisted). **✅ EP.6 난장판 완성**(2026-07-15): `renders/nanjangpan.mp4`(1080×1920, 48.0s), 아카이브 `episodes/nanjangpan/`. 훅=일상어 어원 2연타(EP.5 흥청망청 → 난장판), 반전=정약용 『경세유표』의 "글은 거벽이 짓고 글씨는 사수가 쓰고 정작 본인은 앉아만", 앵커=김홍도 「공원춘효도」의 **우산 바다**(강세황 발문이 春曉라 새벽 배색). 게이트 실증: ① **S3가 conflation 적발** — 초안이 "이틀에 21만 명 vs 정원 33명"을 붙였는데 21만은 1800년 경과 정시(합격 12명), 33명은 식년시 정원으로 **다른 시험**이라 사료에 없는 대비 → 국가유산청이 직접 쓴 "33명에 10만 명" 문장으로 교체(`factcheck.md`에 박제) ② **S4가 "밟혀"→"발표" TTS 오발음 적발**(A/B 2문맥 재현, 발표는 실단어라 파싱 붕괴) → "깔려"로 우회; "강세황"→"강세왕"은 앞에 "스승"을 붙여 임금 오인 차단 ③ **S6.4가 몽둥이·붓 공중 부양 적발** → rig 기하 계산으로 손 부착(지뢰 #19) ④ **S8 블라인드가 우산→"모자 무리"·붓→"창"·돗자리→"나무 상자" 3건 적발** → 전량 재작화 후 재검수 통과(지뢰 #20·21·22). 신규 소품: 우산 바다·응시자 군중·시권 더미·몽둥이(카탈로그 등재). 렌더는 우분투 홈서버(12코어, 25초). 업로드: https://youtu.be/x_Bus8TjxFI (unlisted). **✅ EP.7 신사임당 완성**(2026-07-15): `renders/sin-saimdang.mp4`(1080×1920, 53.3s), 아카이브 `episodes/sin-saimdang/`. 훅=인물 서사·수치 충격(어원 3연타 회피): "조선은 그녀를 현모양처라 부른 적이 없다" — 당대 기록(어숙권 『패관잡기』)은 **화가**로 평했고("포도와 산수가 절묘하니 안견 다음에 간다"), '현모양처'는 **1906년 양규의숙 취지문 초출**의 일본발 수입어로 **그녀가 죽고 355년 뒤**. 풀서클 펀치라인=어숙권이 절묘하다던 그 포도가 지금 5만원권에 실려 있다. 실물 인서트 2점(안견 「몽유도원도」 PD, 신사임당 「초충도」 CC BY 4.0). **후보 문서의 훅("재산 상속")을 S1이 더 강한 훅으로 갈아치운 첫 사례** — 원래 훅은 배경 근거로 흡수. 게이트 실증: ① S3가 "그림 두 점"을 전칭작(傳) 문제로 잡아 "그 포도"로 교체 → 풀서클로 오히려 강화 ② S4가 "서른여덟까지"→"38일까지" 오전사를 잡아 "살" 삽입 ③ **S8 고증이 엽전을 사실 오류로 적발**(지뢰 #27) ④ **S8 블라인드가 지폐 초상을 3연속 불합격**시켜 초상 폐기(지뢰 #25) ⑤ 볏단→시메나와(지뢰 #26) ⑥ 재사용 동헌의 단청·쇼지·기왓골(지뢰 #28) ⑦ 몽유도원도 족자→횡권(지뢰 #29). **S6.5 인간 조정 0건** — 씬별 S6.4 루프가 배치 문제를 먼저 잡아낸 결과(첫 사례). 렌더는 우분투 홈서버(12코어, 27초). 업로드: https://youtu.be/jpHz5LlujUE (unlisted). **✅ EP.8 명량 완성**(2026-07-16): `renders/myeongnyang.mp4`(1080×1920, 61.3s), 아카이브 `episodes/myeongnyang/`. 훅=수치 충격(어원 3연타 회피): 친필 난중일기 「顧見諸將船 則退在遠海觀望不進」로 "13:133이 아니라 한동안 1:133 — 열두 척은 먼 바다로 물러나 관망, 대장선 한 척이 홀로 총통". 펀치라인=승리를 「此實天幸」 네 글자로 자평하고 물때 쓰고 진 옮긴 게 그날 일기의 끝. 왜군은 배 실루엣만(인물 X)으로 고증 회피, 명량 3대 야사(거북선·철쇄·회오리) 그림에서도 배제. 게이트 실증: ① **S3 적대적 검증이 자체 팩트체크가 통과시킨 🔴 적발**(此實天幸=마지막 문장 오보 → 실제는 당사도 이진이 뒤에, 지뢰 #31) ② **S4 TTS 오발음 3건 A/B**(부하→부활·안위야→아니야·칠천량→7천양, 지뢰 #32) ③ **whisper small이 아웃트로 6단어 누락→medium**(지뢰 #33) ④ S6.4가 이순신 갑판 부양·총통 수레 오독 적발(죽절 마디 추가) ⑤ **곶/물 겹침**(사용자 지적 — 곶이 물 밴드 뒤라 공중부양+haze가 잘라먹음 → DOM 순서로 곶을 물 위에, 밑동을 수평선에) ⑥ S8 이중 검수 원패스(블라인드가 판옥선·적함대·난중일기·초요기·청동화포 정명, 고증 🔴 0). 렌더는 로컬(우분투 서버 사용 중, 8코어 34s). 업로드: https://youtu.be/Ao6dmDNrIRE (unlisted). **길이 드리프트 경고: 44.6→48.0→53.3→61.3s — 다음 편은 대본 7줄·씬 7개 이내로 되돌릴 것**(지뢰 #34). **✅ EP.10 한글 창제 완성**(2026-07-18): `renders/hangeul.mp4`(1080×1920, 51.4s), 아카이브 `episodes/hangeul/`. 훅=비밀 프로젝트 반전: "조선 최대의 발명은 실록에 예고 없이 딱 한 문장으로 등장한다"(上親制諺文二十八字 + 과정 기록 0건) + 반전="집현전이 만들어 준 게 아니라 두 달 뒤 집현전 부제학 최만리가 반대 상소". 펀치라인="만든 사람·시기·원리를 풀이한 책까지 남은 세계 유일의 문자". 대본 7줄·48.7s로 길이 드리프트 복귀(지뢰 #34 이행). 게이트 실증: ① **S3 적대적 검증(지뢰 #31 상설화 첫 적용)이 3건 적발** — "딱 한 줄"→"딱 한 문장"(기사는 57자 1문장), 상소 나흘 전(2/16) 집현전 학자 운회 번역 동원 기사(kda_12602016_001)가 있어 주체를 "부제학 최만리"로 특정, "만든 사람·날짜·원리 유일"은 체로키(1821)·파스파(1269) 반례+창제 '날짜' 무기록(是月)으로 반박 노출 → "원리를 풀이한 책까지"로 좁힘(민백·국가유산청 서술 일치) ② S4 A/B: 최만리→[최말리]는 표준 유음화로 TTS 정상(whisper 철자 편향), 네가/내가 동음 중의는 자막+「汝知韻書乎」 스탬프로 방어 ③ S6.4가 달-타이포 겹침·서책 부양(→서가대 신설)·등잔-서안 겹침·어좌 가림(어좌 +150 시프트)·제첨 십자가 오독·"유일" 저대비 적발 ④ **S8 고증이 최만리 흉배를 시대착오로 적발**(문관 흉배=단종 2년 1454 제도, 1444엔 10년 이름) → characters.js `noBadge` 옵션 신설. 익선관 매미날개 부재 지적은 2026-07-08 rig 결정(날개=토끼 귀 오독) 유지로 스킵. 블라인드는 6프레임 전부 정명(왕·신하·상소·일월오봉도·해례본 실물). 실물 인서트: 해례본 표지(간송본, `assets/haerye.jpg` — 사용자 제공, Downloads가 macl로 셸 접근 불가라 사용자가 직접 이동). S6.5 인간 조정 0건(EP.7에 이어 2번째). 렌더 로컬 30초(서버 0W 정지 상태라 부팅 오버헤드>로컬 렌더로 판단). 업로드: https://youtu.be/BLzloEjM6gc (unlisted). **원래 후보 목록(김시습·정약용·광해군·연산군) 전량 소진** → 후속 후보군은 `episode-candidates.md`로 분리(2026-07-14). **EP.10로 후보 표도 전량 소진 — 다음 편은 신규 발굴(1순위 후보: 해례본 1940 발견·간송 전형필, facts F9 확보됨).** 난장판(1순위)도 EP.6로 소진 → **잔여 후보는 전부 🟡(훅 미확정)이고, 어원 훅 3연타는 포맷 피로 위험이라 다음 편은 인물 서사·수치 훅 권장.** 1순위 난장판(과거 시험장 어원, 팩트체크 통과·착수 가능), 2순위 신사임당, 그 외 세종/한글·이순신은 훅 미확정. 어원 훅 후보 4종 팩트체크 결과와 기각 사유(황진이·원효·이판사판·어영부영 등)도 그 문서에 박제 — 재검 금지. **✅ EP.11 간송/해례본 완성**(2026-07-19): `renders/gansong.mp4`(1080×1920, 45.0s), 아카이브 `episodes/gansong/`. 훅=가격 역설("국보를 천 원에 팔겠다기에 부른 값의 열 배를 냈다" — 1천 원 부른 책값에 기와집 열 채 값 만 원, 우리역사넷 "그런 보물은 이런 대접을 받아야"). 근현대(1940~6·25) 문화재 서사라 정치 프레임 아님(EP.9 전례). EP.10 고정 댓글 예고 회수편. 캐스팅: 간송=militia 갓+두루마기(1940s지만 rig 6종 유지, 갓+두루마기 근대 착용 근거로 "조선 양반" 블라인드 판독 — 근현대 인물이라도 별도 rig 안 만들고 기존 6종+색변주로 해결한 첫 사례), 김태준·이용준 실명 미사용(근현대 정치 인접). 게이트 실증: ① **S3 적대적 검증(지뢰 #31)이 자체 대조 통과분 적발** — "세종이 만든 한글의 원리까지 풀이한 단 하나의 책"이 (a)해례(원리해설)=집현전 8인 저술인데 세종 귀속 오독 (b)상주본(2008) 존재로 "단 하나" 과장 → 저술주체 뺀 중립표현으로 수정 ② S4 TTS A/B: "전형필"→whisper "전영필"은 형↔영 철자 편향([저녕필] 자연 ㅎ약화), 실인물 붕괴 아니라 정상(EP.10 최말리 동급) ③ S6.4가 "1만 원" num-세살창 겹침 적발→창 이동 ④ **S8 고증이 사가 한옥 창호 정격자(일본 쇼지) 오독 적발** — EP.7 사가 축소 재사용 시 세로살 8→4로 줄여 균등격자가 됨 → 세로살 8 복원+가로 3연 밴드=세살문(지뢰 #28) ⑤ **S6.5 인간 조정 0건**(EP.7·EP.10 이어 3번째 — 씬별 S6.4 루프 효과). 길이 45.0s로 드리프트 복귀 유지(EP.10 51.4→EP.11 45.0). 렌더 로컬 30초. 신규 소품 3종(지폐 다발·오동나무 상자·닫힌 선장본 고서) props-catalog 등재. **교훈: EP.7 이후 두 번째로 후보표 훅을 S1이 갈아치움("부르는 대로"→"부른 값의 열 배"). 근현대 문화재 소재는 정치 회피하면 채널 소재 범위 확장 가능(실명 인물만 조심).** 업로드: https://youtu.be/NUTa9HWqmps (unlisted). **✅ EP.12 측우기/문종 완성**(2026-07-19): `renders/cheugugi.mp4`(1080×1920, 42.1s), 아카이브 `episodes/cheugugi/`. 훅=세계 최초 우량계의 발명자 반전("만든 사람은 우리가 아는 그 과학자가 아니었다" → 장영실이 아니라 세자=훗날의 문종). EP.1 장영실 팩트체크가 측우기=문종을 대본에서 뺐던 것을 승격(시리즈 연결). **훅 재입찰 3번째 사례**(후보표 훅보다 S1이 찾은 각이 강함, EP.7·11 계보). 게이트 실증: ① **S3 적대적 검증(지뢰 #31)이 자체 대조 통과분 2건 적발** — "세계 최초의 발명품"(중국 진구소 천지분 1247·인도 아르타샤스트라 반례 → 스폰 대사에서 "세계 최초" 빼고 한정형 "세계 최초의 표준 우량계"는 스탬프로만, EP.2/8 플레이북), "손수 만들어"(실록은 고안·측정 명기, 물리적 제작은 서운관 장인 → "손수 고안한"으로 완화, Q1 정합) ② **2차 factcheck(사용자/린터)가 facts.md 2건 정정** — F10 와다 유지(1917) 기원설=오류(와다는 세종/문종으로 정확히 서술, 장영실설은 해방후 1960~70s 위인전 신화화), F11 금영측우기=국보 제329호(2020 승격). 둘 다 대본 미언급이라 영향 0 ③ **S4 TTS: 측우기→whisper "치X기/치국인" 3보이스(InJoon·SunHi·Hyunsu) 재현 = 철자 편향**(정약용→정야경·전형필→전영필 동급, 발음 정상). 화면 "측우기" 스탬프로 식별 보강 ④ **S8이 측우기 대포/망원경 적극 오독 적발**(블라인드+고증 동시) → 짧고 굵은 개방형 청동그릇+측우대로 재작화(지뢰 #36), 재검 3/3 "원통 용기·대포 아님" 통과. 실록 우측 오침 제거(염주 오독 회피). 캐스팅: 장영실=militia 남색관복+갓(EP.1 동일), 세자 문종=king 흑남색 곤룡포 색변주+익선관(왕=홍색 구분, 익선관 매미날개 부재는 rig 확립 결정 유지). 신규 소품: 측우기 재작화(sp4 → 짧고 굵은 개방형+측우대). 재사용: 실록(EP.10 펼친 선장본), 비/먹구름·궁궐 담·박석, king/militia/host rig. **S6.5 인간 조정 0건(EP.7·10·11 이어 4번째)**. 렌더 로컬 30.4초. 업로드: https://youtu.be/_Xit5nnrwNU (unlisted). **다음 후보(episode-candidates): 코끼리 1411(🟢, EP.12 고정댓글 예고, 코끼리 rig 신규작화 필요), 신문고·피휘(🟡).** **✅ EP.13 조선의 코끼리 완성**(2026-07-19): `renders/kokkiri-1411.mp4`(1080×1920, 49.6s), 아카이브 `episodes/kokkiri-1411/`. 훅="실록에는 사람을 죽이고 섬으로 유배당한 코끼리가 있습니다"(EP.12 고정댓글 예고 회수). 신규 rig: **코끼리** — 착수 전 벤치 1회(elephant-bench 3버전 → haiku 3에이전트 블라인드 → v1 채택+앞귀 어두운 톤 보정, 카탈로그 등재), 씬별 인젝터(elephantSVG: 색 변주·코 스왑·눈물 그룹). 게이트 실증: ① **S3 적대적 검증(지뢰 #31)이 엔딩을 뒤집음** — "세종 대의 마지막 명령"의 宣旨 주체가 상왕 태종일 가능성 압도적(실록위키 「선지」 항목 + 3개월 전 「上王從之」)이고 그 명령은 훈훈한 코다가 아니라 **재유배 명령** → 주체 삭제+"다시 섬으로 보내되, 병들어 죽지 않게 하라"로 재구성(더 강한 여운, 수정이 개선이 된 EP.8 계보) ② **길이 드리프트 원인 규명** — 어절수보다 **문장 수(마침표 묵음)**가 지배 변수(89어절 13문장 53.0s → 79어절 9문장 46.9s) ③ S4 A/B: "밟아" 실단어 붕괴 없음(EP.6 "밟혀→발표"와 달리 안전), "이우→이유"는 문맥 의존 whisper 편향 → 「전 공조전서 이우」 스탬프 보강 ④ S6.4 씬별 루프가 5건 적발(실록이 코끼리 머리 가림·부유 섬 실루엣 2건·담 저대비·노을 밴드) ⑤ **S8 고증이 도포 좌임 적발 → 지뢰 #37**(flip-인지형 여밈 fix), 스탬프 한자 5/5 원문 일치, 블라인드 전 프레임 정명(코끼리·실록·돛단배·일월오봉도·대나무·일몰) ⑥ S6.5 인간 조정 0건(5연속). 유배 표현·일본 국왕 표기·7년 기준은 고정 댓글 선제 방어 3종으로. 렌더 로컬 33.5초. 업로드: https://youtu.be/WDP42TWjisg (unlisted). **다음 후보: 신문고(🟡, EP.13 고정댓글 예고 — 훅 재입찰 필요), 피휘(🟡).** **✅ EP.14 착호갑사 완성**(2026-07-21): `renders/chakhogapsa.mp4`(1080×1920, 52.2s), 아카이브 `episodes/chakhogapsa/`. 훅=제도 충격+통념 반전: "조선의 법전에는 호랑이 잡는 부대의 정원이 적혀 있습니다. 사백사십 명." 반전=그런 전문 부대가 있었는데도 호환은 안 끝났고(영조 30년 경기 한 달 120여 명) 사냥꾼 쪽이 죽었다(세조 12년 갑사 박타내, 「誤中, 爲虎所噬」 → 「錄用其子」). 3차 후보표(Gemini) 1순위 채택. **후보표 자산 평가가 틀린 첫 사례** — "호랑이 sp15 ✅ 신규 작화 거의 0"이라 했으나 sp15는 족자 안 까치호랑이 **민화**(평면 회화)라 실제 맹수로 재사용 불가 → 호랑이 rig 신규 벤치(`bench/tiger-bench.html` V1 정립/V2 스토킹/V3 포효, `bench/tiger-bench-results.md`). **부수 발견: 컷아웃 맹수의 위협도는 실루엣이 아니라 입이 만든다** — 같은 몸에 다문 입이면 검수 2명이 독립적으로 "귀엽다/만만하다", 벌린 입+송곳니면 "위험한 맹수". 씬 성격별 얼굴 스왑으로 해결(SKILL 표 등재). 게이트 실증: ① **S3 적대적 검증이 🔴 1건** — "세종 때 스무 명으로 시작"이 세 겹으로 오류(창설 기사 아님·재가는 상왕 태종 「上王從之」·태종 16년 사칭 기사로 이전 존재 입증) → "세종 초엔 스무 명이었는데"로 교체. 덤으로 검증자가 경국대전 원문 DB를 열어 「甲士一萬四千八百員【捉虎四百四十…在額內】」·「捉虎二口者, 除取才許屬」 확보 → 1·4행이 2차 문헌에서 **1차 법전 근거로 승격** ② **서브에이전트 실록 원문 조작 적발**(지뢰 #38)과 **적대적 검증자의 부재 오판 기각**(지뢰 #39) — 양방향 다 틀릴 수 있어 대본 인용은 전량 직접 대조 ③ S4: 착호갑사→whisper "차코갑사"는 격음화 [차코]가 **정상 발음**이라 통과(정약용→정야경 계열 철자 편향), 아웃트로 누락 없음(medium) ④ **S6.4 씬별 루프가 6건 적발**(s7 회전 낙하 프레임 이탈=지뢰 #41, s6 돌담 거품 오독, s1 하단 여백·책 부양, s5 나목→빗자루 오독, 소나무 비례, x슬라이드 오버플로=지뢰 #40) ⑤ **S8 고증이 🔴 과녁을 사실 오류로 적발** — 동심원 다색은 20세기 FITA 표적, 조선은 **관혁(貫革)**(사각 천+중앙 1/3 검은 사각) → 재작화 후 블라인드 "과녁 상". 지뢰 #27(화면 물건도 팩트체크 대상) 계열 ⑥ 스케일 과보정(지뢰 #42) ⑦ **S6.5 인간 조정 0건(6연속)**. 길이 50.7s(대본 8행·81어절·11문장). **지뢰 #34 정정**: 줄바꿈 묵음 0초, 마침표가 지배 변수(실측). 잔여 수용 리스크: 전립(벙거지)은 임란 이후 보편 군모라 조선 전기 씬엔 이름 — rig 유지 택하고 고정 댓글로 선제 방어. 렌더 로컬 36.7초. 업로드: https://youtu.be/YU-k_-yf6GA (unlisted). **다음 후보: 신문고(EP.13·14 두 번 예고 — 이행 권장), 정조어찰첩·세종 관노비 출산휴가(🟢).** **✅ EP.15 신문고 완성**(2026-07-22): `renders/sinmungo.mp4`(1080×1920, 44.0s), 아카이브 `episodes/sinmungo/`. 훅=통념 반전: "억울하면 누구든 두드리라던 북, 신문고. 그런데 아무나 칠 순 없었습니다" — **EP.13·14 고정댓글에서 두 번 예고한 것을 회수**(예고 이행 첫 2연속 사례). 반전 구조: 3단계 관문(수령→관찰사→사헌부) → 실이용층은 한양 양반·사연은 노비/재산 → 역설(억울함을 들어준다면서 직소 길은 이 북 하나로 좁힘) → 백성이 실제로 쓴 건 징(격쟁). 1차 근거 승격: 태종 2년 교서 「外方則呈守令監司, 不爲究治, 則呈司憲府, 司憲府不爲究治, 乃來擊鼓」(kca_10201026_002)·「非其越訴 … 乃許擊鼓」(kca_10201026_003)·「敢行誣告者反坐」(kca_10108001_001)·영조 47년 복설 「復設申聞鼓於昌德宮進善門」(kua_14711023_003) — **전량 sillok 직접 fetch 대조**(지뢰 #38 이행). 게이트 실증: ① **S3 적대적 검증이 ❌ 0건 / ⚠️ 2건 적발** — (a) "죄다 틀어막았다"는 억제 일면(신문고는 왕 전달 창구이기도) → "이 북 하나로 좁혀 놓았다"로 완화 (b) "그래서 백성은 징을" = 격쟁 성행은 16C 중엽~라 150년 압축, 태종~문종대엔 오히려 격쟁 금지 → "훗날 신문고마저 유명무실해지자"로 시점 분리. **후보표가 경고한 "무용지물 단정 과장" 리스크가 정확히 이 두 형태로 현실화** ② **S8 블라인드가 신문고를 "징"으로 판독 → 배럴 재작화(지뢰 #43)**. 후보표의 "북 sp9 ✅ 리컬러면 됨" 자산 평가가 틀렸다(EP.14 호랑이 민화 계보 — **자산 재사용 가능성은 카탈로그 이름이 아니라 시그니처로 판정할 것**) ③ **inspect clipped_text 오검출 = 전환 중 stage(지뢰 #44)** — JSON rect로 진단, 해당 씬 슬라이드 0.45→0.2s ④ S4 TTS 아웃트로 누락 0(medium), 사헌부→"사원부"는 whisper 철자편향(스탬프 「司憲府」로 보강) ⑤ **S6.5 인간 조정 0건(7연속)**. 신규 소품: **신문고 배럴 드럼**(drumSVG 인젝터 — s1 히어로/s3/s4 3회 재사용), **징**(황동 원반+울림통 보스+고리+채), **관아 동헌**, **3단계 화살표 스탬프 체인**, **홍살문 빗장**. 재사용: 가마(sp12 royal 리컬러), 교서/문서 두루마리, king/militia/host rig. 길이 44.0s로 드리프트 복귀 유지. 렌더 로컬 30.9초. 업로드: https://youtu.be/lyM4E_qXvuE (unlisted). **다음 후보: 정조어찰첩(EP.15 고정댓글 예고, 🟢), 세종 관노비 출산휴가(🟢).** **✅ EP.16 정조어찰첩 완성**(2026-07-23): `renders/jeongjo-eochal.mp4`(1080×1920, 49.1s), 아카이브 `episodes/jeongjo-eochal/`. 훅=**"조선의 성군 정조는 편지에 몇 번이고 덧붙였습니다. 보는 즉시 찢어 없애라."** — EP.15 고정댓글 예고 회수. **훅 재입찰 4번째**(EP.7·11·12 계보): 후보표 훅(비속어 299통)보다 "왕이 태우라 한 편지를 신하가 태우지 않았다"가 훅·펀치라인을 한 소재로 닫는다. 민백 서술("받는 즉시 없애라고 누차 언급하였으나, 심환지는 수신일자까지 부기하여 빠짐없이 보관")이 그대로 근거. **이 소재는 실록에 없다**(개인 소장 서첩) — 실록 대조가 구조적으로 불가해 기관 해설(민백·국가유산포털)+2009년 공개 당시 1차 보도로 이중 확인(지뢰 #38 변형 적용). 펀치라인은 사용자 선택으로 **양립 착지**("성군의 뒷담화도, 그걸로 일을 되게 한 솜씨도") — 1행의 "성군"을 회수하는 수미상관. 게이트 실증: ① **후보표가 2건 틀렸음을 S1/S3가 적발** — (a) "독살설이 학계에서 폐기됨"은 **사실 아님**(이덕일 정면 반박 + 같은 어찰을 피살설 근거로 읽는 보도까지 병존 → 전면 배제) (b) 「胡種子」 대상은 심환지가 아니라 **서용보**(커뮤니티 최다 오귀속) ② **S3 적대검증 🔴 1건** — "**신하가** 아뢰고"가 정조 치세 전체의 전칭 주장이 됨(최성환 2011의 권도·군신의리 해석이 정면 반대, 안대회 본인이 "심환지뿐 아니라 많은 신하와 편지" 명시) → **"심환지가 아뢰고"**로 주어 한정. ❌ 1건 "사 년"(실제 3년 10개월) → 검증된 "죽기 열흘 남짓 전까지"로 교체하며 오히려 강화(EP.8 계보) ③ **S4 TTS 붕괴 2건 A/B 3/3 재현** — "열사흘"→**"14일"**(숫자 의미 반전), "벽파"→**"벽화"**(시파까지 벽화로) → 둘 다 대사에서 빼고 스탬프 전담(EP.2 금오 플레이북) ④ **S6.4가 벽 밴드 z-순서 버그 적발**(지뢰 #49 — check 0에러인데 소품 3개가 화면에서 사라져 있었음) ⑤ **S8 블라인드 확신도 상 오독 3건**(지뢰 #45·46·47: 창호→감옥창살, 글줄→십자가판, 글로우→달) 재작화 후 전량 해소 ⑥ **S8 고증 🔴 4건 중 3건 수정**(사모 각 부양 — 블라인드도 독립 적발해 `characters.js` 소급 수정 / 동헌 지붕→우산 / 오봉병 소나무·폭포 누락). **익선관 뿔 지적은 2026-07-08 rig 결정(뿔=토끼 귀 오독) 유지로 스킵** — EP.10에 이은 2번째, 고정 댓글로 선제 방어 ⑦ **S6.5 인간 조정 0건(8연속)**. 길이 47.4s. 신규 소품: 간찰(접힘 자국 시그니처)·어찰첩 6첩+선반장. 렌더 로컬 34.9초. 업로드: https://youtu.be/c6g8Z6wezYE (unlisted). **다음 후보: 심환지 후일담(🟢 신규 — "임금의 편지를 안 태운 남자가 임금의 유산을 지웠다", EP.16 고정댓글 예고), 세종 관노비 출산휴가(🟢).** **✅ EP.17 심환지 후일담/장용영 혁파 완성**(2026-07-24): `renders/simhwanji.mp4`(1080×1920, 48.9s), 아카이브 `episodes/simhwanji/`. 훅=**"읽고 나면 없애라 한 편지를 날짜까지 적어 남긴 그 신하가, 정조가 세운 호위 군영 장용영을 없애자고 아뢴다 — 근거는 정조가 예전에 이렇게 말했다는 그의 기억이었다."** EP.16 고정댓글 예고 회수(예고 이행 3연속). **훅 재입찰 5번째**(EP.7·11·12·16 계보): 후보표 훅("유산을 지웠다")보다 **'없애는 근거가 고인의 육성이었고, 그것도 한 사람이 전한 회고였다'**가 압도적. 1차 근거 전량 직접 대조(`kwa_10201020_002`/`wwa_10201020_002` 「命罷壯勇營。 領議政沈煥之啓言」, 정조 하교 「記昔…若曰: 予之此擧, 非得已也。 今左右暬御, 皆出一時權宜, 後世不可以此爲法」, 「或曰 三年無改…此則未達之論也」, 「皆無異辭」, 「積有時月」, 「依大臣所奏擧行」, 영의정 임명 `kwa_10007004_006`). 게이트 실증: ① **S3 적대적 검증이 🔴 2건 적발해 대본 전면 개고** — (a) 「今左右暬御」를 "이건"으로 뭉갠 것이 **1802년 심환지가 한 확대 해석을 화면이 그대로 복창**하는 구조였다(대본이 심환지에게 낚인 채 심환지를 고발). 暬御를 복원하고 **그 확대 자체를 새 반전으로 승격** (b) 「若曰」=회고 전언인데 "정조가 남긴 말"로 단정 → "그의 기억"으로 교체. **수정이 곧 강화(EP.8 계보) 두 번째 사례** ② ⚠️ 3건 완화(命=대왕대비 / 「積有時月」로 '그날은 낭독'임 명시 / 외영은 총리영으로 존속) ③ **독립 리서치 2건이 위키 날짜 오류 2개 교정**(우의정 1795→1798 `kva_12208028_001`, 관작 추탈 4/4→4/3 `kwa_10604003_001`) — 그중 하나는 검증자가 "재확인 실패"로 유보한 「積有時月」을 본 세션이 직접 페치로 확정(지뢰 #39 역방향 이행) ④ **S4 전사 파이프라인 교체**(지뢰 #53 — hyperframes transcribe가 한글을 �로 깨고 아웃트로를 뭉개서 faster-whisper 직접 생성으로) ⑤ **S6.4 씬별 루프가 6건 적발**(수렴이 어좌를 통째로 가림, 인용 타이포↔창호 겹침, 성곽 과소·저대비, 상단 절반 공백 2건, 정적 폭 1086>1080=지뢰 #40) ⑥ **S8 블라인드가 🔴 3건**(지뢰 #50 빈 어좌→문짝/북, #51 반투명 군중→유령, #52 깃대 관통) → 전량 수정, 신규 소품 **수렴(발)은 A·B 독립 "발/주렴 — 상" 통과**. 길이 48.9s. 렌더 로컬 29.0초/1467프레임. **다음 후보: 세종 관노비 출산휴가(🟢, EP.17 고정댓글 예고), 신주무원록(🟢).** **✅ EP.19 신주무원록/조선의 검시 제도 완성**(2026-07-25): `renders/sinju-muwonrok.mp4`(1080×1920, 47.6s), 아카이브 `episodes/sinju-muwonrok/`, 업로드 https://youtu.be/uj1gu7oCAmQ. 훅=**"조선에선 살인 사건이 나면 시신을 두 번 검사했다. 두 검시관이 만나 입을 맞추면 볼기 예순 대."** EP.18 고정댓글 예고 회수(예고 이행 4연속). **훅 재입찰 6번째**(EP.7·11·12·16·17 계보): 후보표 훅(은비녀·식초·2심제 나열)보다 **독립 이중검시**가 훅·반전·펀치를 한 소재로 닫는다. 1차 근거 전량 직접 대조 — 세종 14/2/16 `kda_11402016_004`「檢屍者, 人之死生係焉…或不親檢, 皆委於吏」, 21/2/6 `kda_12102006_005`(검시장식 전국 반포), 24/2/27 `kda_12402027_003`「初檢官…在外所在官守令。復檢官…在外隣近守令」, 28/5/15 `kda_12805015_005`「詳覆司專掌初復檢狀內同異, 相考施行」. 게이트 실증: ① **S3 적대검증이 🔴 2건**(형조=서울 절차 오적용 → 기관명 삭제 / 신분 예외가 법전 명문이라 은폐 시 대본 전체가 거짓말 → 6행으로 노출) **+ 근거 승격 1건** — 모호한 "알릴 수 없었다"를 검증자가 찾아낸 **『대명률직해』 436조**「初覆檢官吏相見, 符同屍狀…正官杖六十」로 바꿔 **수치·처벌로 강화**(수정이 곧 강화, EP.8·17 계보 3번째) ② **S8 고증이 대본까지 되돌림 — "곤장"이 시대착오**(곤은 임란 이후 군문 형구·속대전 1746 규정, 대명률 杖은 가는 막대) → **"볼기"로 교체 + TTS·transcript·자막·씬 타이밍 전면 재생성**(EP.14 과녁 계보) ③ **가짜 한자 3곳 적발**(현판·표석·제첨을 손으로 그린 획) → SVG `<text>`로 東軒/界/無寃錄 ④ 고증 🔴 추가 4건(형틀=허리 높이 중국 가구 / 옹기=서양 오크통 / 선장본 좌철 / 서안·소반=서양 트레슬) 전량 재작화 ⑤ **블라인드 4라운드** — 옹기·은비녀·벼루·책·표석·지붕은 통과로 전환, **형틀+장은 3연속 오독 후 소품 제거**(고증이 요구하는 '가는 장'은 무맥락에서 판독 불가 — EP.6 붓 제거 계보) ⑥ **S6.5 인간 조정 0건(9연속)**. **신규 지뢰 2종: #54(char-tuning.js 미로드 — lint 0에러로 통과), z-순서 버그 2건(종이가 은비녀를 덮음·소나무가 담장 관통, 인스펙터 미검출).** 길이 46.6s. 렌더 로컬 45.9초. **다음 후보: 문종화차(🟡, EP.19 고정댓글 예고), 화랑 화장(🟡), 발굴 백로그(사약의 진실·잡상=서유기·허준 해부 픽션 등).** **✅ EP.20 문종화차 완성**(2026-07-25): `renders/munjong-hwacha.mp4`(1080×1920, 47.1s), 아카이브 `episodes/munjong-hwacha/`, 업로드 https://youtu.be/X7JZjYvwl0E (unlisted). 훅=**"불을 붙이면 로켓 백 발이 차례로 날아가는 수레, 설계자는 조선의 임금이었습니다"** — EP.19 고정댓글 예고 회수(예고 이행 5연속), EP.12(측우기=세자 문종) 시리즈 직결(s2 콜백). 펀치=병약 통념 반전 + 짐수레 유머(「宜當無事時, 分授各司, 令轉輸雜物」). 1차 근거 직접 대조: `kea_10102013_002`(「先是上命臨瀛大君璆, 制火車 … 揷置中神機箭一百箇, 或置四箭銃筒五十箇, 以火炷之, 連次而發 … 其制度, 皆上所指授」), 승하일 `kea_10205014_001`. 게이트 실증: ① **S3 적대검증 — 대본 6행 전원 ✅(이례적 원패스), 유일 수정은 스탬프 한정형**("세계에서 가장 오래된 로켓 병기 기록" → 민백 원문대로 "가장 **자세하고** 오래된" — 한정어 탈락 시 『화룡경』 반례에 뚫림). 후보표의 "권11 결본이라 실록 단정 금지" 경고는 과도했음(화차 기사 실재) ② S4 실단어 붕괴 0건 — "치구기"(측우기)·"즉이"(즉위)는 철자 편향, **"이명대군"은 임영대군의 정상 연음**(차코갑사 계보) ③ S6.4 씬별 루프 5건(글로우 원반·측우기 과소·수레 스케일·東軒 현판 가림·로켓 궤적 부유 원) ④ **S8 블라인드 2라운드 — 수정 6건**: 글로우 밴드 전면 제거(**저채도 0.2로 낮춰도 "공중 원반/렌더 오류" 판독** — 지뢰 #47 확장, 스탬프 뒤 배경도 예외 아님), s1 총통 제거(**단독 판독은 "술통"** — sp10 벤치 통과는 포수 앙상블 덕), s3 목재를 수레 적재로(지면 분리 더미=부양 오독), s6 소나무 실루엣+노을 밴드 평탄화(**별하늘+로켓+곡선 노을 = "화성 표면" 오독**), s6 신기전 장전(**무장전 격자판은 야간에 "와플/초콜릿바"** — 무기 단서는 장전 화살이 전담), 고립 연기 원 제거. **기각 2건 — 검수 지적도 검증 대상**(지뢰 #39 계열): "발사 방향 불일치" 주장은 확대 검증 결과 화살·로켓 모두 우상향 일치(오판), "도포 좌임" 주장은 픽셀 확대로 우임 확인(그늘 안깃을 겉깃으로 오인 — 지뢰 #37 분기 정상 작동) ⑤ 전립·익선관은 EP.14·EP.10 선례대로 rig 유지+고정댓글 방어 ⑥ **S6.5 인간 조정 0건(10연속)**. 길이 47.1s(69어절 10문장). 렌더 로컬 32초. **다음 후보: 화랑 화장(🟡, EP.20 고정댓글 예고 — 훅 각도 재설계 필요), 백로그 승격 검토(사약의 진실·잡상=서유기).**
4. 유의: 반복 재사용 애니 + AI 내레이션 조합은 YouTube 대량생산 콘텐츠 정책 심사 대상이 될 수 있음 — 대본의 관점/유머가 차별화 포인트.

## 세션 이력 요약 (2026-07-04)

리서치(기성 툴 조사: rhubarb-lip-sync, PyToon, synctoon, Adobe Character Animator 등) → "JSON 장면 스크립트 + hyperframes 렌더" 자체 파이프라인 결정 → 파일럿 구현·검증·렌더까지 한 세션에 완료. 립싱크는 rhubarb 대신 whisper 단어 타이밍 기반 0.1s 입 플랩(사우스파크식)으로 단순화 — 결과 충분.
